apiVersion: v1
data:
  RECOMMENDATION_ADDR: recommendation-service
  RECOMMENDATION_PORT: "8080"
  CATALOG_ADDR: catalog-service
  CATALOG_PORT: "8080"
kind: ConfigMap
metadata:
  name: reports-endpoint-config
  namespace: backend
