// Code generated by genny. DO NOT EDIT.
// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/mauricelam/genny

package check

import (
	"fmt"
	"sort"
	"strings"

	"github.com/stackrox/rox/generated/storage"
)

// If you want to add a set for your custom type, simply add another go generate line along with the
// existing ones. If you're creating a set for a primitive type, you can follow the example of "string"
// and create the generated file in this package.
// For non-primitive sets, please make the generated code files go outside this package.
// Sometimes, you might need to create it in the same package where it is defined to avoid import cycles.
// The permission set is an example of how to do that.
// You can also specify the -imp command to specify additional imports in your generated file, if required.

// *storage.Policy represents a generic type that we want to have a set of.

// StoragePolicySet will get translated to generic sets.
type StoragePolicySet map[*storage.Policy]struct{}

// Add adds an element of type *storage.Policy.
func (k *StoragePolicySet) Add(i *storage.Policy) bool {
	if *k == nil {
		*k = make(map[*storage.Policy]struct{})
	}

	oldLen := len(*k)
	(*k)[i] = struct{}{}
	return len(*k) > oldLen
}

// AddMatching is a utility function that adds all the elements that match the given function to the set.
func (k *StoragePolicySet) AddMatching(matchFunc func(*storage.Policy) bool, elems ...*storage.Policy) bool {
	oldLen := len(*k)
	for _, elem := range elems {
		if !matchFunc(elem) {
			continue
		}
		if *k == nil {
			*k = make(map[*storage.Policy]struct{})
		}
		(*k)[elem] = struct{}{}
	}
	return len(*k) > oldLen
}

// AddAll adds all elements of type *storage.Policy. The return value is true if any new element
// was added.
func (k *StoragePolicySet) AddAll(is ...*storage.Policy) bool {
	if len(is) == 0 {
		return false
	}
	if *k == nil {
		*k = make(map[*storage.Policy]struct{})
	}

	oldLen := len(*k)
	for _, i := range is {
		(*k)[i] = struct{}{}
	}
	return len(*k) > oldLen
}

// Remove removes an element of type *storage.Policy.
func (k *StoragePolicySet) Remove(i *storage.Policy) bool {
	if len(*k) == 0 {
		return false
	}

	oldLen := len(*k)
	delete(*k, i)
	return len(*k) < oldLen
}

// RemoveAll removes the given elements.
func (k *StoragePolicySet) RemoveAll(is ...*storage.Policy) bool {
	if len(*k) == 0 {
		return false
	}

	oldLen := len(*k)
	for _, i := range is {
		delete(*k, i)
	}
	return len(*k) < oldLen
}

// RemoveMatching removes all elements that match a given predicate.
func (k *StoragePolicySet) RemoveMatching(pred func(*storage.Policy) bool) bool {
	if len(*k) == 0 {
		return false
	}

	oldLen := len(*k)
	for elem := range *k {
		if pred(elem) {
			delete(*k, elem)
		}
	}
	return len(*k) < oldLen
}

// Contains returns whether the set contains an element of type *storage.Policy.
func (k StoragePolicySet) Contains(i *storage.Policy) bool {
	_, ok := k[i]
	return ok
}

// Cardinality returns the number of elements in the set.
func (k StoragePolicySet) Cardinality() int {
	return len(k)
}

// IsEmpty returns whether the underlying set is empty (includes uninitialized).
//
// Deprecated: use Cardinality() == 0 instead
func (k StoragePolicySet) IsEmpty() bool {
	return len(k) == 0
}

// Clone returns a copy of this set.
func (k StoragePolicySet) Clone() StoragePolicySet {
	if k == nil {
		return nil
	}
	cloned := make(map[*storage.Policy]struct{}, len(k))
	for elem := range k {
		cloned[elem] = struct{}{}
	}
	return cloned
}

// Difference returns a new set with all elements of k not in other.
func (k StoragePolicySet) Difference(other StoragePolicySet) StoragePolicySet {
	if len(k) == 0 || len(other) == 0 {
		return k.Clone()
	}

	retained := make(map[*storage.Policy]struct{}, len(k))
	for elem := range k {
		if !other.Contains(elem) {
			retained[elem] = struct{}{}
		}
	}
	return retained
}

// Helper function for intersections.
func (k StoragePolicySet) getSmallerLargerAndMaxIntLen(other StoragePolicySet) (smaller StoragePolicySet, larger StoragePolicySet, maxIntLen int) {
	maxIntLen = len(k)
	smaller, larger = k, other
	if l := len(other); l < maxIntLen {
		maxIntLen = l
		smaller, larger = larger, smaller
	}
	return smaller, larger, maxIntLen
}

// Intersects returns whether the set has a non-empty intersection with the other set.
func (k StoragePolicySet) Intersects(other StoragePolicySet) bool {
	smaller, larger, maxIntLen := k.getSmallerLargerAndMaxIntLen(other)
	if maxIntLen == 0 {
		return false
	}
	for elem := range smaller {
		if _, ok := larger[elem]; ok {
			return true
		}
	}
	return false
}

// Intersect returns a new set with the intersection of the members of both sets.
func (k StoragePolicySet) Intersect(other StoragePolicySet) StoragePolicySet {
	smaller, larger, maxIntLen := k.getSmallerLargerAndMaxIntLen(other)
	if maxIntLen == 0 {
		return nil
	}

	retained := make(map[*storage.Policy]struct{}, maxIntLen)
	for elem := range smaller {
		if _, ok := larger[elem]; ok {
			retained[elem] = struct{}{}
		}
	}
	return retained
}

// Union returns a new set with the union of the members of both sets.
func (k StoragePolicySet) Union(other StoragePolicySet) StoragePolicySet {
	if len(k) == 0 {
		return other.Clone()
	} else if len(other) == 0 {
		return k.Clone()
	}

	underlying := make(map[*storage.Policy]struct{}, len(k)+len(other))
	for elem := range k {
		underlying[elem] = struct{}{}
	}
	for elem := range other {
		underlying[elem] = struct{}{}
	}
	return underlying
}

// Equal returns a bool if the sets are equal
func (k StoragePolicySet) Equal(other StoragePolicySet) bool {
	thisL, otherL := len(k), len(other)
	if thisL == 0 && otherL == 0 {
		return true
	}
	if thisL != otherL {
		return false
	}
	for elem := range k {
		if _, ok := other[elem]; !ok {
			return false
		}
	}
	return true
}

// AsSlice returns a slice of the elements in the set. The order is unspecified.
func (k StoragePolicySet) AsSlice() []*storage.Policy {
	if len(k) == 0 {
		return nil
	}
	elems := make([]*storage.Policy, 0, len(k))
	for elem := range k {
		elems = append(elems, elem)
	}
	return elems
}

// GetArbitraryElem returns an arbitrary element from the set.
// This can be useful if, for example, you know the set has exactly one
// element, and you want to pull it out.
// If the set is empty, the zero value is returned.
func (k StoragePolicySet) GetArbitraryElem() (arbitraryElem *storage.Policy) {
	for elem := range k {
		arbitraryElem = elem
		break
	}
	return arbitraryElem
}

// AsSortedSlice returns a slice of the elements in the set, sorted using the passed less function.
func (k StoragePolicySet) AsSortedSlice(less func(i, j *storage.Policy) bool) []*storage.Policy {
	slice := k.AsSlice()
	if len(slice) < 2 {
		return slice
	}
	// Since we're generating the code, we might as well use sort.Sort
	// and avoid paying the reflection penalty of sort.Slice.
	sortable := &sortableStoragePolicySlice{slice: slice, less: less}
	sort.Sort(sortable)
	return sortable.slice
}

// Clear empties the set
func (k *StoragePolicySet) Clear() {
	*k = nil
}

// Freeze returns a new, frozen version of the set.
func (k StoragePolicySet) Freeze() FrozenStoragePolicySet {
	return NewFrozenStoragePolicySetFromMap(k)
}

// ElementsString returns a string representation of all elements, with individual element strings separated by `sep`.
// The string representation of an individual element is obtained via `fmt.Fprint`.
func (k StoragePolicySet) ElementsString(sep string) string {
	if len(k) == 0 {
		return ""
	}
	var sb strings.Builder
	first := true
	for elem := range k {
		if !first {
			sb.WriteString(sep)
		}
		fmt.Fprint(&sb, elem)
		first = false
	}
	return sb.String()
}

// NewStoragePolicySet returns a new thread unsafe set with the given key type.
func NewStoragePolicySet(initial ...*storage.Policy) StoragePolicySet {
	underlying := make(map[*storage.Policy]struct{}, len(initial))
	for _, elem := range initial {
		underlying[elem] = struct{}{}
	}
	return underlying
}

type sortableStoragePolicySlice struct {
	slice []*storage.Policy
	less  func(i, j *storage.Policy) bool
}

func (s *sortableStoragePolicySlice) Len() int {
	return len(s.slice)
}

func (s *sortableStoragePolicySlice) Less(i, j int) bool {
	return s.less(s.slice[i], s.slice[j])
}

func (s *sortableStoragePolicySlice) Swap(i, j int) {
	s.slice[j], s.slice[i] = s.slice[i], s.slice[j]
}

// A FrozenStoragePolicySet is a frozen set of *storage.Policy elements, which
// cannot be modified after creation. This allows users to use it as if it were
// a "const" data structure, and also makes it slightly more optimal since
// we don't have to lock accesses to it.
type FrozenStoragePolicySet struct {
	underlying map[*storage.Policy]struct{}
}

// NewFrozenStoragePolicySetFromMap returns a new frozen set from the set-style map.
func NewFrozenStoragePolicySetFromMap(m map[*storage.Policy]struct{}) FrozenStoragePolicySet {
	if len(m) == 0 {
		return FrozenStoragePolicySet{}
	}
	underlying := make(map[*storage.Policy]struct{}, len(m))
	for elem := range m {
		underlying[elem] = struct{}{}
	}
	return FrozenStoragePolicySet{
		underlying: underlying,
	}
}

// NewFrozenStoragePolicySet returns a new frozen set with the provided elements.
func NewFrozenStoragePolicySet(elements ...*storage.Policy) FrozenStoragePolicySet {
	underlying := make(map[*storage.Policy]struct{}, len(elements))
	for _, elem := range elements {
		underlying[elem] = struct{}{}
	}
	return FrozenStoragePolicySet{
		underlying: underlying,
	}
}

// Contains returns whether the set contains the element.
func (k FrozenStoragePolicySet) Contains(elem *storage.Policy) bool {
	_, ok := k.underlying[elem]
	return ok
}

// Cardinality returns the cardinality of the set.
func (k FrozenStoragePolicySet) Cardinality() int {
	return len(k.underlying)
}

// IsEmpty returns whether the underlying set is empty (includes uninitialized).
func (k FrozenStoragePolicySet) IsEmpty() bool {
	return len(k.underlying) == 0
}

// AsSlice returns the elements of the set. The order is unspecified.
func (k FrozenStoragePolicySet) AsSlice() []*storage.Policy {
	if len(k.underlying) == 0 {
		return nil
	}
	slice := make([]*storage.Policy, 0, len(k.underlying))
	for elem := range k.underlying {
		slice = append(slice, elem)
	}
	return slice
}

// AsSortedSlice returns the elements of the set as a sorted slice.
func (k FrozenStoragePolicySet) AsSortedSlice(less func(i, j *storage.Policy) bool) []*storage.Policy {
	slice := k.AsSlice()
	if len(slice) < 2 {
		return slice
	}
	// Since we're generating the code, we might as well use sort.Sort
	// and avoid paying the reflection penalty of sort.Slice.
	sortable := &sortableStoragePolicySlice{slice: slice, less: less}
	sort.Sort(sortable)
	return sortable.slice
}

// ElementsString returns a string representation of all elements, with individual element strings separated by `sep`.
// The string representation of an individual element is obtained via `fmt.Fprint`.
func (k FrozenStoragePolicySet) ElementsString(sep string) string {
	if len(k.underlying) == 0 {
		return ""
	}
	var sb strings.Builder
	first := true
	for elem := range k.underlying {
		if !first {
			sb.WriteString(sep)
		}
		fmt.Fprint(&sb, elem)
		first = false
	}
	return sb.String()
}

// The following functions make use of casting `k.underlying` into a mutable Set. This is safe, since we never leak
// references to these objects, and only invoke mutable set methods that are guaranteed to return a new copy.

// Union returns a frozen set that represents the union between this and other.
func (k FrozenStoragePolicySet) Union(other FrozenStoragePolicySet) FrozenStoragePolicySet {
	if len(k.underlying) == 0 {
		return other
	}
	if len(other.underlying) == 0 {
		return k
	}
	return FrozenStoragePolicySet{
		underlying: StoragePolicySet(k.underlying).Union(other.underlying),
	}
}

// Intersect returns a frozen set that represents the intersection between this and other.
func (k FrozenStoragePolicySet) Intersect(other FrozenStoragePolicySet) FrozenStoragePolicySet {
	return FrozenStoragePolicySet{
		underlying: StoragePolicySet(k.underlying).Intersect(other.underlying),
	}
}

// Difference returns a frozen set that represents the set difference between this and other.
func (k FrozenStoragePolicySet) Difference(other FrozenStoragePolicySet) FrozenStoragePolicySet {
	return FrozenStoragePolicySet{
		underlying: StoragePolicySet(k.underlying).Difference(other.underlying),
	}
}

// Unfreeze returns a mutable set with the same contents as this frozen set. This set will not be affected by any
// subsequent modifications to the returned set.
func (k FrozenStoragePolicySet) Unfreeze() StoragePolicySet {
	return StoragePolicySet(k.underlying).Clone()
}
