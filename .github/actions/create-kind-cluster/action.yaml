name: Create KinD cluster with images
description: >
  Creates a KinD cluster and pre-pulls container images into the node's
  containerd via ctr, eliminating kubelet image-pull backoff on the critical
  path. Runs in the background — returns immediately. The cluster and images
  become available asynchronously; callers should wait on the readiness file
  and/or retry kubectl until the cluster is ready.

  File-based outputs:
    /tmp/kind-ready     — touched when the KinD cluster + central-db image are ready
    /tmp/kind-create.log — full creation/pull log

inputs:
  cluster-name:
    description: Unique cluster name
    required: true
  tag:
    description: Image tag for central-db and main
    required: true
  registry:
    description: Image registry
    default: quay.io/rhacs-eng
  memory-limit:
    description: Docker container memory cap for the KinD node
    default: 12g
  pull-scanner:
    description: Also pre-pull scanner and scanner-db images (tag read from SCANNER_VERSION)
    default: "false"
  quay-user:
    description: Quay.io username
    required: true
  quay-pass:
    description: Quay.io password
    required: true

runs:
  using: composite
  steps:
    - name: Create KinD cluster (background)
      shell: bash
      run: |
        set +x
        echo "${{ inputs.quay-pass }}" | docker login -u "${{ inputs.quay-user }}" --password-stdin quay.io
        set -x

        SCANNER_TAG=""
        if [[ "${{ inputs.pull-scanner }}" == "true" && -f SCANNER_VERSION ]]; then
          SCANNER_TAG=$(cat SCANNER_VERSION)
        fi

        (
          NODE="${{ inputs.cluster-name }}-control-plane"
          # Store credentials once — never trace them.
          CTR_AUTH="${{ inputs.quay-user }}:${{ inputs.quay-pass }}"

          kind create cluster --name "${{ inputs.cluster-name }}"

          docker update --memory "${{ inputs.memory-limit }}" \
            --memory-swap "${{ inputs.memory-limit }}" "$NODE"

          # Pull central-db first (foreground wait) — it is the slowest kubelet
          # backoff failure mode. Everything else pulls in the background.
          # set +x around ctr pull to keep --user credentials out of the trace.
          set +x
          docker exec "$NODE" ctr -n k8s.io images pull \
            --user "$CTR_AUTH" --platform linux/amd64 \
            "${{ inputs.registry }}/central-db:${{ inputs.tag }}" &
          PID_CDB=$!
          docker exec "$NODE" ctr -n k8s.io images pull \
            --user "$CTR_AUTH" --platform linux/amd64 \
            "${{ inputs.registry }}/main:${{ inputs.tag }}" &
          if [[ -n "$SCANNER_TAG" ]]; then
            docker exec "$NODE" ctr -n k8s.io images pull \
              --user "$CTR_AUTH" --platform linux/amd64 \
              "${{ inputs.registry }}/scanner:${SCANNER_TAG}" &
            docker exec "$NODE" ctr -n k8s.io images pull \
              --user "$CTR_AUTH" --platform linux/amd64 \
              "${{ inputs.registry }}/scanner-db:${SCANNER_TAG}" &
          fi
          set -x

          wait $PID_CDB
          touch /tmp/kind-ready
        ) > /tmp/kind-create.log 2>&1 &

        echo "KinD cluster creation started in background"
