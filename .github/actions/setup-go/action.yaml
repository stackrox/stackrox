name: Setup Go and Cache
description: >
  Activate the best available Go from the runner's hostedtoolcache,
  override setup-go's problematic defaults, and manage GOCACHE with
  stale-entry trimming and mtime stabilization for test caching.
inputs:
  save:
    description: Whether this job saves caches on pushes to the default branch.
    required: false
    default: 'true'
  key-suffix:
    description: Extra suffix for cache keys (use to distinguish matrix entries that share github.job).
    required: false
    default: ''
  use-setup-go:
    description: >
      Use actions/setup-go instead of direct hostedtoolcache lookup.
      Slower (~8s overhead) but delegates Go installation to the
      upstream action. Set to 'true' as a fallback if the default
      path has issues.
    required: false
    default: 'false'
runs:
  using: composite
  steps:
    # Activate the latest Go from hostedtoolcache that satisfies go.mod.
    # This replaces actions/setup-go — we avoid its downsides:
    #
    # Problems with actions/setup-go:
    #   - Sets GOTOOLCHAIN=local, which prevents sub-module tools from
    #     auto-downloading their required Go versions.
    #   - Caches GOCACHE with a single key shared across all jobs.
    #     Different jobs (unit tests, postgres tests, style) write
    #     incompatible entries into the same cache, and it grows
    #     unbounded because setup-go never trims stale entries.
    #   - Reads the `go` directive instead of `toolchain`, so it
    #     installs the minimum language version (e.g. 1.25.0) even
    #     when go.mod specifies a newer build version via `toolchain`.
    #
    # What this action does instead:
    #   - Sets GOTOOLCHAIN=auto so sub-modules can resolve their own
    #     toolchain requirements.
    #   - Keys GOCACHE per job (and per matrix entry via key-suffix)
    #     so jobs don't overwrite each other, and trims stale entries
    #     in a post-step before cache save.
    #   - Uses go.mod's `toolchain` directive to determine the build
    #     version (falls back to `go` if unset), and finds the best
    #     matching Go pre-installed on the runner.
<<<<<<< HEAD
    - name: Setup Go
      run: |
        # Prefer toolchain directive (build version), fall back to go directive (minimum).
        # The toolchain directive strips the "go" prefix: "toolchain go1.25.9" → "1.25.9".
        required=$(grep '^toolchain ' go.mod | awk '{print $2}' | sed 's/^go//')
        if [[ -n "$required" ]]; then
          echo "go.mod toolchain directive: go$required"
        else
          required=$(grep '^go ' go.mod | awk '{print $2}')
          echo "::warning::No toolchain directive in go.mod — falling back to go directive ($required). Consider adding 'toolchain go<version>' to go.mod to pin the build version."
        fi
        best=""
        for d in $(ls -d /opt/hostedtoolcache/go/*/x64 2>/dev/null | sort -rV); do
=======
    # Resolve Go version from go.mod: prefer toolchain directive (build
    # version), fall back to go directive (minimum language version).
    - name: Resolve Go version
      id: go-version
      run: |
        version=$(grep '^toolchain ' go.mod | awk '{print $2}' | sed 's/^go//' || true)
        if [[ -n "$version" ]]; then
          echo "go.mod toolchain directive: go$version"
        else
          version=$(grep '^go ' go.mod | awk '{print $2}' || true)
          if [[ -n "$version" ]]; then
            echo "::warning::No toolchain directive in go.mod — falling back to go directive ($version). Consider adding 'toolchain go<version>' to go.mod to pin the build version."
          else
            echo "::error::Could not parse Go version from go.mod (no toolchain or go directive found)"
            exit 1
          fi
        fi
        echo "version=$version" >> "$GITHUB_OUTPUT"
      shell: bash

    # Default path: activate Go from hostedtoolcache (~0.3s).
    - name: Activate hostedtoolcache Go
      if: inputs.use-setup-go != 'true'
      run: |
        required="${{ steps.go-version.outputs.version }}"
        arch=$(uname -m | sed 's/x86_64/x64/;s/aarch64/arm64/')
        best=""
        # Check /opt (host runner) and /mnt/opt (inside container jobs that mount /opt:/mnt/opt).
        for d in $(ls -d /opt/hostedtoolcache/go/*/$arch /mnt/opt/hostedtoolcache/go/*/$arch 2>/dev/null | sort -rV); do
>>>>>>> davdhacs/setup-go-combined
          ver=$(basename "$(dirname "$d")")
          if [ -x "$d/bin/go" ] && printf '%s\n%s\n' "$required" "$ver" | sort -V -C; then
            best="$d"
            break
          fi
        done
<<<<<<< HEAD
=======
        echo "::add-matcher::${GITHUB_ACTION_PATH}/matchers.json"
>>>>>>> davdhacs/setup-go-combined
        if [[ -n "$best" ]]; then
          echo "$best/bin" >> "$GITHUB_PATH"
          [[ ":$PATH:" == *":$HOME/go/bin:"* ]] || echo "$HOME/go/bin" >> "$GITHUB_PATH"
          echo "GOTOOLCHAIN=auto" >> "$GITHUB_ENV"
<<<<<<< HEAD
          echo "::add-matcher::${GITHUB_ACTION_PATH}/matchers.json"
=======
>>>>>>> davdhacs/setup-go-combined
          echo "Using $("$best/bin/go" version) from $best"
        else
          echo "::warning::No suitable Go in hostedtoolcache (need >=$required), using system Go"
        fi
      shell: bash

<<<<<<< HEAD
=======
    # Fallback path: delegate to actions/setup-go (~8s).
    - name: Install Go via actions/setup-go
      if: inputs.use-setup-go == 'true'
      uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # ratchet:actions/setup-go@v5
      with:
        go-version: ${{ steps.go-version.outputs.version }}
        cache: false

    - name: Override GOTOOLCHAIN
      if: inputs.use-setup-go == 'true'
      run: echo "GOTOOLCHAIN=auto" >> "$GITHUB_ENV"
      shell: bash

>>>>>>> davdhacs/setup-go-combined
    - name: Determine Go cache paths
      id: cache-paths
      run: |
        echo "GOCACHE=$(go env GOCACHE)" >> "$GITHUB_OUTPUT"
        echo "GOARCH=$(go env GOARCH)" >> "$GITHUB_OUTPUT"
        echo "TAG=${{ github.sha }}" >> "$GITHUB_OUTPUT"
        # Hash go.mod once for use in cache keys. When dependencies change,
        # the GOCACHE must be invalidated because Go's module index caches
        # parsed imports by (filename, mtime, size) and our fixed mtimes
        # can cause stale cached imports when file content changes.
        echo "GOMOD_HASH=$(sha256sum go.mod | cut -c1-16)" >> "$GITHUB_OUTPUT"
        suffix="${{ inputs.key-suffix }}"
        echo "KEY_SUFFIX=${suffix:+-$suffix}" >> "$GITHUB_OUTPUT"  # prepends dash if set
      shell: bash

    - name: Cache Go Build (save)
      if: inputs.save == 'true' && (github.event_name == 'push' && github.ref_name == github.event.repository.default_branch)
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # ratchet:actions/cache@v5
      with:
        path: ${{ steps.cache-paths.outputs.GOCACHE }}
        key: go-build-v2-${{ github.job }}${{ steps.cache-paths.outputs.KEY_SUFFIX }}-${{ steps.cache-paths.outputs.GOARCH }}-${{ steps.cache-paths.outputs.GOMOD_HASH }}-${{ steps.cache-paths.outputs.TAG }}
        restore-keys: go-build-v2-${{ github.job }}${{ steps.cache-paths.outputs.KEY_SUFFIX }}-${{ steps.cache-paths.outputs.GOARCH }}-${{ steps.cache-paths.outputs.GOMOD_HASH }}-

    - name: Cache Go Build (restore)
      if: ${{ !(inputs.save == 'true' && (github.event_name == 'push' && github.ref_name == github.event.repository.default_branch)) }}
      uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # ratchet:actions/cache/restore@v5
      with:
        path: ${{ steps.cache-paths.outputs.GOCACHE }}
        key: go-build-v2-${{ github.job }}${{ steps.cache-paths.outputs.KEY_SUFFIX }}-${{ steps.cache-paths.outputs.GOARCH }}-${{ steps.cache-paths.outputs.GOMOD_HASH }}-${{ steps.cache-paths.outputs.TAG }}
        restore-keys: go-build-v2-${{ github.job }}${{ steps.cache-paths.outputs.KEY_SUFFIX }}-${{ steps.cache-paths.outputs.GOARCH }}-${{ steps.cache-paths.outputs.GOMOD_HASH }}-

    - name: Mark GOCACHE entries for stale detection
      run: |
        gocache="${{ steps.cache-paths.outputs.GOCACHE }}"
        if [[ -d "$gocache" ]]; then
          # Backdate all cache entries to year 2000. During the build/test,
          # Go's markUsed() updates mtimes of accessed entries to "now"
          # (it always updates when mtime is >1 hour old). The post-step
          # below trims entries still at year 2000 before cache save.
          timeout 120 find "$gocache" -type f -exec touch -t 200001010000 {} + || true
          # Protect trim.txt: if backdated, Go's built-in Trim() sees
          # "last trim was in year 2000" and deletes ALL backdated entries
          # before the build starts. Setting it to now prevents this.
          echo "$(date +%s)" > "$gocache/trim.txt"
          echo "Marked GOCACHE entries for stale detection"
        fi
      shell: bash

    # Registered AFTER cache steps so the post-step runs BEFORE cache save
    # (GHA runs post-steps in reverse registration order).
    - name: Trim stale GOCACHE entries (post-step)
      uses: gacts/run-and-post-run@598d7a875d5620e0457490555b5e18e46082aa47 # v1.4.0
      with:
        post: >
          set +e;
          gocache="$(go env GOCACHE)";
          [[ -d "$gocache" ]] && [[ -f "$gocache/trim.txt" ]] || { echo "Skipping GOCACHE trim"; exit 0; };
          before=$(du -sm "$gocache" 2>/dev/null | cut -f1);
          cutoff=$(mktemp); touch -t 200001020000 "$cutoff";
          find "$gocache" -type f ! -newer "$cutoff" -delete 2>/dev/null || true;
          rm -f "$cutoff";
          find "$gocache" -mindepth 1 -type d -empty -delete 2>/dev/null || true;
          after=$(du -sm "$gocache" 2>/dev/null | cut -f1);
          echo "GOCACHE trimmed: ${before:-0}MB -> ${after:-0}MB (removed $((${before:-0} - ${after:-0}))MB)"

    - name: Clear test cache on non-PR runs
      if: github.event_name != 'pull_request'
      run: |
        # Non-PR runs (master, release branches, nightlies) always run tests
        # from scratch to catch real failures. go clean -testcache removes
        # cached test results while preserving the compilation cache.
        go clean -testcache
        echo "Cleared test cache for fresh run"
      shell: bash

    - name: Stabilize file mtimes for Go test cache
      run: |
        # Go's test cache validates inputs by (path, size, mtime) — NOT content.
        # git checkout sets all mtimes to "now", which differs between CI runs,
        # causing every test to miss the cache. Setting a fixed mtime makes the
        # test cache hit across runs when file content hasn't changed.
        #
        # Go's module index also caches parsed imports by (filename, mtime, size).
        # A fixed mtime can cause stale cached imports when a file changes content
        # but keeps the same size. This is mitigated by including go.mod hash in
        # the GOCACHE key — dependency changes invalidate the entire cache.
        # See: https://github.com/golang/go/blob/go1.25.7/src/cmd/go/internal/modindex/read.go#L116

        git ls-files | xargs touch -t 200101010000
        # Also stabilize directory mtimes — tests using os.ReadDir or
        # filepath.Walk check directory mtimes too.
        git ls-files | sed 's|/[^/]*$||' | sort -u | xargs touch -t 200101010000
      shell: bash
