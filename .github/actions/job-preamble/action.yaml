name: Job Preamble
description: Common steps for most jobs
inputs:
  free-disk-space:
    description: 'Free disk space desired in GB'
    required: false
    default: 20
  gcp-account:
    description: 'Account to be used to upload tests data'
    required: true
runs:
  using: composite
  steps:
    - name: Check installed tools
      shell: bash
      run: |
        set +e
        set -x
        gcloud version && which gcloud
        bq version && which bq
        true

    - name: Remove unused gha runner tools if found
      shell: bash
      run: |
        set +e
        set -x
        df --si /
        cleanup=(
          /usr/share/dotnet
          /usr/share/miniconda
          /usr/share/swift
          /usr/share/kotlinc
          /opt/ghc
          /opt/hostedtoolcache/CodeQL
          /opt/hostedtoolcache/Ruby
          /opt/az
          /opt/hostedtoolcache/CodeQL
          /usr/local/lib/android

          /mnt/usr/local/lib/node_modules
          /mnt/opt/hostedtoolcache/node
          /mnt/opt/pipx
          /mnt/opt/hostedtoolcache/Python
          /mnt/opt/hostedtoolcache/go
        )
        for d in "${cleanup[@]}"; do
          free=$(df -BG --output=avail / | tail -1)
          if [[ ${free%G} -gt "${{ inputs.free-disk-space }}" ]]; then
            echo "Reached required free disk space [${{ inputs.free-disk-space }}]"
            break
          fi
          if [[ -d "/mnt${d}" ]]; then
            rm -rf "/mnt${d}"
          elif [[ -d "$d" ]]; then
            rm -rf "$d" \
              || sudo rm -rf "$d"
          fi
        done
        df --si /

    - name: Recover docker image cache space
      run: |
        free=$(df -BG --output=avail / | tail -1)
        if [[ ${free%G} -gt "${{ inputs.free-disk-space }}" ]]; then
          echo "Skipping docker prune. Already reached required free disk space [${{ inputs.free-disk-space }}]"
          exit
        fi
        printf 'Docker prune: '
        docker system prune --force --all
        df --si /
      shell: bash

    - name: Ignore dubious repository ownership
      run: |
        # Prevent fatal error "detected dubious ownership in repository" from recent git.
        git config --global --add safe.directory "$(pwd)"
      shell: bash

    - name: Auth gcloud
      uses: 'google-github-actions/auth@v2'
      with:
        credentials_json: '${{ inputs.gcp-account }}'
        export_environment_variables: true
        create_credentials_file: true
        cleanup_credentials: false

    - name: Check gcloud CLI version
      id: gcloud-version-check
      continue-on-error: true
      shell: bash
      run: |
        set -x
        gcloud info
        version=$(gcloud info --format="value(basic.version)")
        echo "version=${version}" >> "$GITHUB_OUTPUT"
        [[ ${version%%.*} -ge 363 ]]

    - name: Install gcloud CLI
      if: steps.gcloud-version-check.outcome == 'failure'
      uses: 'google-github-actions/setup-gcloud@v2'
      with:
        version: '>= 363.0.0'  # does not check installed gcloud version. checks toolcache for previously installed and cached

    - name: Record job info
      uses: gacts/run-and-post-run@d803f6920adc9a47eeac4cb6c93dbc2e2890c684 # v1.4.0
      env:
        GCP_SERVICE_ACCOUNT_STACKROX_CI: ${{ inputs.gcp-account }}
      if: "${{ env.GCP_SERVICE_ACCOUNT_STACKROX_CI != '' }}"
      with:
        shell: bash
        run: >
          source scripts/ci/lib.sh;
          create_job_record "${{ github.job }}" "gha";
        post: >
          source scripts/ci/lib.sh;
          update_job_record outcome "${{ job.status }}" stopped_at "CURRENT_TIMESTAMP()";
          df --si /;
