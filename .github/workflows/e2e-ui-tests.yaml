name: e2e-ui-tests

on:
  workflow_call:
    inputs:
      should-run:
        description: "true if should really run the job. false - otherwise, i.e. the job should be skipped."
        required: true
        type: boolean
      tag:
        description: "Image tag to test."
        required: true
        type: string
      ci-job-name:
        description: "Unique job name, e.g. gke-ui-e2e-tests. Used to configure the image prefetcher."
        required: true
        type: string
      cluster-name-prefix:
        description: "Used as prefix in provisioned cluster names. Maximum 20 characters."
        required: true
        type: string
      use-konflux-images:
        description: "Whether to use Konflux-built images."
        type: boolean
        default: false

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  gke-ui-e2e-tests:
    if: github.event_name == 'workflow_dispatch' || inputs.should-run
    runs-on: ubuntu-latest
    env:
      MAIN_IMAGE_TAG: ${{ inputs.tag }}
      BUILD_TAG: ${{ inputs.tag }}
      CI_JOB_NAME: ${{ inputs.ci-job-name }}
      ARTIFACT_DIR: ./junit-reports
      LOAD_BALANCER: lb
      ORCHESTRATOR_FLAVOR: k8s
      KUBERNETES_PROVIDER: gke
      OUTPUT_FORMAT: helm
      SCANNER_V4_DB_STORAGE_CLASS: stackrox-gke-ssd
      SENSOR_SCANNER_SUPPORT: "true"
      USE_KONFLUX_IMAGES: ${{ inputs.use-konflux-images && 'true' || '' }}
      USE_ROXIE_DEPLOY: "true"
      PYTHONUNBUFFERED: "1"
      GCP_SERVICE_ACCOUNT_STACKROX_CI: ${{ secrets.GCP_SERVICE_ACCOUNT_STACKROX_CI }}
      CLUSTER_NAME_PREFIX: "${{ inputs.cluster-name-prefix }}"
    timeout-minutes: 150
    steps:

    - name: Validate workflow input
      run: |
        if [[ "${#CLUSTER_NAME_PREFIX}" -gt 20 ]]; then
          echo "::error::cluster-name-prefix must not exceed 20 characters (is ${#CLUSTER_NAME_PREFIX})."
          exit 1
        fi

    - name: Checkout repo
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # ratchet:actions/checkout@v7.0.0
      with:
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.sha || github.sha }}
        persist-credentials: false

    - uses: ./.github/actions/job-preamble
      with:
        gcp-account: ${{ secrets.GCP_SERVICE_ACCOUNT_STACKROX_CI }}

    - name: Docker login to Quay.io
      env:
        REGISTRY_USERNAME: ${{ secrets.QUAY_RHACS_ENG_RO_USERNAME }}
        REGISTRY_PASSWORD: ${{ secrets.QUAY_RHACS_ENG_RO_PASSWORD }}
      run: docker login -u "${REGISTRY_USERNAME}" --password-stdin quay.io <<<"${REGISTRY_PASSWORD}"

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # ratchet:google-github-actions/setup-gcloud@v3
      with:
        install_components: "gke-gcloud-auth-plugin,beta"

    - name: Create GKE cluster
      env:
        MACHINE_TYPE: e2-standard-8
        NUM_NODES: "3"
        GKE_SPOT: "true"
      timeout-minutes: 25
      run: |
        scripts/ci/gke.sh provision_gke_cluster "$CLUSTER_NAME_PREFIX"
        echo "KUBECONFIG=${HOME}/.kube/config" >> "$GITHUB_ENV"

    - name: Retrieving roxie version from repository
      id: roxie-version
      run: echo "version=$(cat ROXIE_VERSION)" >> "$GITHUB_OUTPUT"

    - name: Install roxie
      uses: stackrox/actions/roxie/install-cli@43bb707aa4f32ead5bfb8d5ab11fb409f971ffc8 # ratchet:stackrox/actions/roxie/install-cli@v1
      with:
        version: "v${{ steps.roxie-version.outputs.version }}"

    - name: Verify kubectl access and wait for cluster to stabilize
      timeout-minutes: 5
      run: |
        kubectl cluster-info
        kubectl get nodes
        scripts/ci/gke.sh wait_for_cluster

    - name: Create artifact directory
      run: mkdir -p "$ARTIFACT_DIR"

    - name: Run pre-test
      shell: python
      env:
        QUAY_RHACS_ENG_BEARER_TOKEN: ${{ secrets.QUAY_RHACS_ENG_BEARER_TOKEN }}
        QUAY_RHACS_ENG_RO_USERNAME: ${{ secrets.QUAY_RHACS_ENG_RO_USERNAME }}
        QUAY_RHACS_ENG_RO_PASSWORD: ${{ secrets.QUAY_RHACS_ENG_RO_PASSWORD }}
      run: |
        import sys
        sys.path.append('.openshift-ci')
        from pre_tests import PreSystemTests
        PreSystemTests().run()

    - name: Run UI E2E tests
      env:
        QUAY_RHACS_ENG_RO_USERNAME: ${{ secrets.QUAY_RHACS_ENG_RO_USERNAME }}
        QUAY_RHACS_ENG_RO_PASSWORD: ${{ secrets.QUAY_RHACS_ENG_RO_PASSWORD }}
        REGISTRY_USERNAME: ${{ secrets.QUAY_RHACS_ENG_RO_USERNAME }}
        REGISTRY_PASSWORD: ${{ secrets.QUAY_RHACS_ENG_RO_PASSWORD }}
      timeout-minutes: 120
      run: tests/e2e/run-ui-e2e.sh

    - name: Collect UI test artifacts
      if: always()
      run: |
        # Copy standalone platform test JUnit reports
        if [[ -d "ui/test-results/reports/cypress/integration" ]]; then
          mkdir -p "$ARTIFACT_DIR/ui-standalone"
          cp -r ui/test-results/reports/cypress/integration/. "$ARTIFACT_DIR/ui-standalone/" || true
        fi

        # Copy OCP console plugin reports (if they exist - won't on GKE)
        if [[ -d "ui/test-results/reports/cypress/integration-ocp" ]]; then
          mkdir -p "$ARTIFACT_DIR/ui-ocp-plugin"
          cp -r ui/test-results/reports/cypress/integration-ocp/. "$ARTIFACT_DIR/ui-ocp-plugin/" || true
        fi

        # Copy Cypress artifacts (videos, screenshots, coverage)
        if [[ -d "ui/test-results/artifacts" ]]; then
          mkdir -p "$ARTIFACT_DIR/ui-artifacts"
          cp -r ui/test-results/artifacts/. "$ARTIFACT_DIR/ui-artifacts/" || true
        fi

    - name: Run post-test
      if: always()
      shell: python
      run: |
        import sys
        sys.path.append('.openshift-ci')
        from post_tests import PostClusterTest
        post = PostClusterTest(
            check_stackrox_logs=True,
        )
        post.openshift_namespaces = []  # GKE, not OpenShift
        post.run(['/tmp/ui-e2e-misc-logs', '${{ env.ARTIFACT_DIR }}'])

    - name: Run final-post
      if: always()
      shell: python
      run: |
        import sys
        sys.path.append('.openshift-ci')
        from post_tests import FinalPost
        FinalPost(store_qa_tests_data=False).run()

    - name: Teardown GKE cluster
      if: always()
      continue-on-error: true
      run: |
        if [[ -z "${CLUSTER_NAME:-}" ]]; then
          echo "No cluster to teardown"
          exit 0
        fi
        if ! scripts/ci/gke.sh teardown_gke_cluster false; then
          echo "::warning::WARNING: Could not tear down cluster ${CLUSTER_NAME}. See Teardown step logs for details."
          exit 6
        fi

    - name: Publish summary for failed tests
      uses: test-summary/action@37b508cfee6d4d080eedd00b5bb240a6a784a6a5 # ratchet:test-summary/action@v2
      if: always()
      with:
        paths: ${{ env.ARTIFACT_DIR }}/**/*.xml
        show: "fail"

    - name: Publish summary for passed and skipped tests
      uses: test-summary/action@37b508cfee6d4d080eedd00b5bb240a6a784a6a5 # ratchet:test-summary/action@v2
      if: always()
      with:
        paths: ${{ env.ARTIFACT_DIR }}/**/*.xml
        show: "pass, skip"

    - name: Report junit failures in jira
      if: (!cancelled())
      id: junit2jira
      uses: ./.github/actions/junit2jira
      with:
        create-jiras: ${{ github.event_name == 'push' }}
        jira-user: ${{ secrets.JIRA_USER }}
        jira-token: ${{ secrets.JIRA_TOKEN }}
        gcp-account: ${{ secrets.GCP_SERVICE_ACCOUNT_STACKROX_CI }}
        directory: ${{ env.ARTIFACT_DIR }}
