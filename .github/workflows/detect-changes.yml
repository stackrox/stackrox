name: Detect changed files

# Checks which files changed in a PR and reports per-filter results.
# All filter keys default to true (changed) and are only set to false
# after confirming no matching files changed. This means:
#   - Push events (no PR): all true, every job runs.
#   - API failure or 3000+ files: all true, every job runs.
#   - Missing key (e.g. job renamed): null != false → true, job runs.
# Jobs use: if: fromJSON(needs.detect-changes.outputs.changed).key != false
# The != false is required so that missing keys (null) default to run.

on:
  workflow_call:
    inputs:
      filters:
        description: >
          JSON object mapping labels to arrays of .gitignore-style
          pathspec patterns (* = single segment, ** = recursive).
          Labels are shared across jobs via fromJSON(changed).label.
          Example: { "go": ["**/*.go", "go.mod"], "ui": ["ui/**"] }
        type: string
        required: true
    outputs:
      changed:
        description: >
          JSON object with true/false per filter key. Keys default
          to true and are set to false only when confirmed unchanged.
          Check with: fromJSON(changed).key != false
        value: ${{ jobs.detect.outputs.changed }}

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.compare.outputs.changed }}
    steps:
      - name: Compare list of files changed with filters
        id: compare
        shell: bash
        env:
          FILTERS: ${{ inputs.filters }}
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -eo pipefail

          glob_to_regex() {
            local regex="${1//\./\\.}"
            regex="${regex//\*\*/@@GLOBSTAR@@}"
            regex="${regex//\*/[^/]*}"
            regex="${regex//@@GLOBSTAR@@\//(.+/)?}"
            regex="${regex//\/@@GLOBSTAR@@//.+}"
            regex="${regex//@@GLOBSTAR@@/.+}"
            echo "^${regex}$"
          }

          # Default: all keys true (assume changed, must run).
          all_true=$(echo "${FILTERS}" | jq -c 'to_entries | map({(.key): true}) | add')

          if [[ -z "${PR_NUMBER}" ]]; then
            echo "Not a PR, all changed" >&2
            echo "changed=${all_true}" | tee -a "${GITHUB_OUTPUT}"
            exit 0
          fi

          if ! gh_output="$(gh api "repos/${GH_REPO}/pulls/${PR_NUMBER}/files" --paginate --jq '.[].filename')"; then
            echo "::warning::GitHub API call failed, assuming all changed" >&2
            echo "changed=${all_true}" | tee -a "${GITHUB_OUTPUT}"
            exit 0
          fi

          mapfile -t files < <(echo -n "${gh_output}")
          echo "Changed files (${#files[@]}):" >&2
          printf '  %s\n' "${files[@]}" >&2

          if [[ ${#files[@]} -ge 3000 ]]; then
            echo "::warning::PR has 3000+ changed files, assuming all changed" >&2
            echo "changed=${all_true}" | tee -a "${GITHUB_OUTPUT}"
            exit 0
          fi

          # Start with all true, set to false only when confirmed unchanged.
          declare -A results
          for k in $(echo "${FILTERS}" | jq -r 'keys[]'); do
            results["$k"]=true
          done

          for k in $(echo "${FILTERS}" | jq -r 'keys[]'); do
            mapfile -t patterns < <(echo "${FILTERS}" | jq -r --arg k "$k" '.[$k][]')
            found=false
            for p in "${patterns[@]}"; do
              regex=$(glob_to_regex "$p")
              for f in "${files[@]}"; do
                if [[ $f =~ $regex ]]; then
                  found=true
                  break 2
                fi
              done
            done
            if ! $found; then
              results["$k"]=false
            fi
            echo "${k}: ${results[$k]}" >&2
          done

          result=$(for k in "${!results[@]}"; do echo "${k} ${results[$k]}"; done \
            | jq -Rsc 'split("\n") | map(select(. != "")) | map(split(" ") | {(.[0]): (.[1] == "true")}) | add')
          echo "changed=${result}" | tee -a "${GITHUB_OUTPUT}"
