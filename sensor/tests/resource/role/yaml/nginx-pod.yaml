apiVersion: v1
kind: Pod
metadata:
  name: nginx-rogue
  labels:
    app: nginx-rogue
spec:
  containers:
  - name: nginx
    image: quay.io/rhacs-eng/qa-multi-arch:nginx-1.21.1
    ports:
    - containerPort: 80
