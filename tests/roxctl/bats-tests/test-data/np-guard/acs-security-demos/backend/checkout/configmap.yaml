apiVersion: v1
data:
  RECOMMENDATION_ADDR: recommendation-service
  RECOMMENDATION_PORT: "8080"
  NOTIFICATION_ADDR: notification-service
  NOTIFICATION_PORT: "8080"
  GATEWAY_ADDR: gateway-service.payments
  GATEWAY_PORT: "8080"
kind: ConfigMap
metadata:
  name: checkout-endpoint-config
  namespace: backend