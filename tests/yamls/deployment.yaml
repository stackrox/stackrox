apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  app: nginx
spec:
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
