# Will be used within roxctl_verification.sh.
# Contains a Job resource.
apiVersion: batch/v1
kind: Job
metadata:
  name: nginx
spec:
  template:
    spec:
      containers:
      - name: nginx
        image: nginx
