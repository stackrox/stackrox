spec:
  template:
    spec:
      containers:
      - name: scanner
        resources:
          requests:
            memory: "500Mi"
            cpu: "500m"
          limits:
            memory: "2500Mi"
            cpu: "2000m"
