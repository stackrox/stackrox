nodeSelector:
  kubernetes.io/arch: arm64

collector:
  nodeSelector:
    kubernetes.io/arch: arm64

admissionControl:
  nodeSelector:
    kubernetes.io/arch: arm64

sensor:
  nodeSelector:
    kubernetes.io/arch: arm64