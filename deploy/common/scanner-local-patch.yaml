spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: scanner
        resources:
          requests:
            memory: "500Mi"
            cpu: "100m"
          limits:
            cpu: null
            memory: "2500Mi"
