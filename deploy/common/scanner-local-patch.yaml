spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: scanner
        resources:
          requests:
            cpu: "250m"
            memory: "500Mi"
          limits:
            cpu: null
            memory: "2500Mi"
