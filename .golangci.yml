run:
  timeout: 2m

linters-settings:
  gocritic:
    enabled-tags:
      - diagnostic
      - experimental
      - opinionated
      - performance
      - style
    disabled-checks:
      - dupImport # https://github.com/go-critic/go-critic/issues/845
      - commentFormatting
      - octalLiteral
      - unnamedResult
      - unnecessaryDefer
      - importShadow
      - emptyStringTest
  nolintlint:
    allow-leading-space: true # don't require machine-readable nolint directives (i.e. with no leading space)
    allow-unused: false # report any unused nolint directives
    require-explanation: false # don't require an explanation for nolint directives
    require-specific: false # don't require nolint directives to be specific about which linter is being skipped

linters:
  # please, do not use `enable-all`: it's deprecated and will be removed soon.
  # inverted configuration with `enable-all` and `disable` is not scalable during updates of golangci-lint
  disable-all: true
  enable:
    - asciicheck
    # - bodyclose
    # - deadcode
    # - depguard
    # - dogsled
    # - dupl
    # - errcheck
    # - funlen
    # - gochecknoglobals
    # - gochecknoinits
    # - gocognit
    # - goconst
    - exportloopref
    # - gocritic TODO: add in follow-up
    # - gocyclo
    # - godot
    # - godox
    # - goerr113
    # - gofmt TODO: see if we can migrate our gofmt to this
    # - goimports TODO: see if we can migrate our goimports to this
    # - golint TODO: see if we can migrate our golint to this
    # - gomnd
    # - goprintffuncname
    # - gosec
    - gosimple
    # - govet TODO: see if we can migrate our govet to this
    - ineffassign
    # - interfacer
    # - lll
    # - maligned
    # - misspell
    # - nakedret TODO: add in follow-up
    # - nestif
    - nolintlint
    # - prealloc
    - rowserrcheck
    # - scopelint
    # - staticcheck
    # - structcheck
    # - stylecheck
    # - testpackage
    # - typecheck
    # - unconvert TODO: add in follow-up
    # - unparam TODO: add in follow-up
    # - unused
    # - varcheck
    # - whitespace
    # - wsl
