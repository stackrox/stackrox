apply plugin: 'groovy'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
	mavenCentral()
}

tasks.withType(Test) {
	testLogging {
		showStandardStreams true
		exceptionFormat "full"
	}
}
