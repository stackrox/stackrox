apiVersion: platform.stackrox.io/v1alpha1
kind: SecuredCluster
metadata:
  labels:
    app.kubernetes.io/name: rhacs-operator
    app.kubernetes.io/managed-by: kustomize
  name: securedcluster-sample
spec:
  # TODO(user): Add fields here
