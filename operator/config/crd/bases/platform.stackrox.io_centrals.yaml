
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: centrals.platform.stackrox.io
spec:
  group: platform.stackrox.io
  names:
    kind: Central
    listKind: CentralList
    plural: centrals
    singular: central
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Central is the configuration template for the central services.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CentralSpec defines the desired state of Central
            properties:
              central:
                description: CentralComponentSpec defines settings for the "central"
                  component.
                properties:
                  adminPasswordSecret:
                    description: Reference to a user-created Secret with admin password
                      stored in data item "value". If omitted, the operator will instead
                      auto-generate a password, create such secret and expose the
                      name of that secret (if it is different from the name "central-admin-password")
                      in status.central.generatedAdminPasswordSecret
                    properties:
                      name:
                        description: The name of the referenced secret.
                        type: string
                    required:
                    - name
                    type: object
                  defaultTLSSecret:
                    description: A Kubernetes secret that contains a TLS certificate
                      and key for HTTPS serving of the web UI.
                    properties:
                      name:
                        description: The name of the referenced secret.
                        type: string
                    required:
                    - name
                    type: object
                  exposure:
                    description: Exposure defines how central is exposed.
                    properties:
                      loadBalancer:
                        description: ExposureLoadBalancer defines settings for exposing
                          central via a LoadBalancer.
                        properties:
                          enabled:
                            type: boolean
                          ip:
                            type: string
                          port:
                            format: int32
                            type: integer
                        type: object
                      nodePort:
                        description: ExposureNodePort defines settings for exposing
                          central via a NodePort.
                        properties:
                          enabled:
                            type: boolean
                          port:
                            format: int32
                            type: integer
                        type: object
                      route:
                        description: ExposureRoute defines settings for exposing central
                          via a Route.
                        properties:
                          enabled:
                            type: boolean
                        type: object
                    type: object
                  nodeSelector:
                    additionalProperties:
                      type: string
                    type: object
                  persistence:
                    description: Persistence defines persistence settings for central.
                    properties:
                      hostPath:
                        description: HostPathSpec defines settings for host path config.
                        properties:
                          path:
                            type: string
                        type: object
                      persistentVolumeClaim:
                        description: PersistentVolumeClaim defines PVC-based persistence
                          settings.
                        properties:
                          claimName:
                            type: string
                          size:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          storageClassName:
                            type: string
                        type: object
                    type: object
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: 'Limits describes the maximum amount of compute
                          resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: 'Requests describes the minimum amount of compute
                          resources required. If Requests is omitted for a container,
                          it defaults to Limits if that is explicitly specified, otherwise
                          to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                        type: object
                    type: object
                type: object
              customize:
                description: Customizations to apply on all central components.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Custom annotations to set on all objects apart from
                      Pods.
                    type: object
                  envVars:
                    description: Custom environment variables to set on pods' containers.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: Name of the environment variable. Must be a
                            C_IDENTIFIER.
                          type: string
                        value:
                          description: 'Variable references $(VAR_NAME) are expanded
                            using the previous defined environment variables in the
                            container and any service environment variables. If a
                            variable cannot be resolved, the reference in the input
                            string will be unchanged. The $(VAR_NAME) syntax can be
                            escaped with a double $$, ie: $$(VAR_NAME). Escaped references
                            will never be expanded, regardless of whether the variable
                            exists or not. Defaults to "".'
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    TODO: Add other useful fields. apiVersion, kind,
                                    uid?'
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                            fieldRef:
                              description: 'Selects a field of the pod: supports metadata.name,
                                metadata.namespace, `metadata.labels[''<KEY>'']`,
                                `metadata.annotations[''<KEY>'']`, spec.nodeName,
                                spec.serviceAccountName, status.hostIP, status.podIP,
                                status.podIPs.'
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                            resourceFieldRef:
                              description: 'Selects a resource of the container: only
                                resources limits and requests (limits.cpu, limits.memory,
                                limits.ephemeral-storage, requests.cpu, requests.memory
                                and requests.ephemeral-storage) are currently supported.'
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    TODO: Add other useful fields. apiVersion, kind,
                                    uid?'
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: Custom labels to set on all objects apart from Pods.
                    type: object
                type: object
              egress:
                description: Egress defines settings related to outgoing network traffic.
                properties:
                  connectivityPolicy:
                    description: ConnectivityPolicy is a type for values of spec.egress.connectivityPolicy.
                    type: string
                type: object
              imagePullSecrets:
                items:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
                type: array
              scanner:
                description: ScannerComponentSpec defines settings for the "scanner"
                  component.
                properties:
                  analyzer:
                    description: ScannerAnalyzerComponent describes the analyzer component
                    properties:
                      nodeSelector:
                        additionalProperties:
                          type: string
                        type: object
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                            type: object
                        type: object
                      scaling:
                        description: ScannerAnalyzerScaling defines replication settings
                          of the analyzer.
                        properties:
                          autoScaling:
                            description: AutoScalingPolicy is a type for values of
                              spec.scannerSpec.replicas.autoScaling.
                            type: string
                          maxReplicas:
                            description: Defaults to 5
                            format: int32
                            type: integer
                          minReplicas:
                            description: Defaults to 2
                            format: int32
                            type: integer
                          replicas:
                            description: Defaults to 3
                            format: int32
                            type: integer
                        type: object
                    type: object
                  db:
                    description: DeploymentSpec defines settings that affect a deployment.
                    properties:
                      nodeSelector:
                        additionalProperties:
                          type: string
                        type: object
                      resources:
                        description: ResourceRequirements describes the compute resource
                          requirements.
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Limits describes the maximum amount of compute
                              resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: 'Requests describes the minimum amount of
                              compute resources required. If Requests is omitted for
                              a container, it defaults to Limits if that is explicitly
                              specified, otherwise to an implementation-defined value.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                            type: object
                        type: object
                    type: object
                  scannerComponent:
                    description: Defaults to Enabled
                    type: string
                type: object
              tls:
                description: TLSConfig defines common TLS-related settings for all
                  components.
                properties:
                  additionalCAs:
                    items:
                      description: AdditionalCA defines a certificate for an additional
                        Certificate Authority.
                      properties:
                        content:
                          description: PEM format
                          type: string
                        name:
                          description: Must be a valid file basename
                          type: string
                      required:
                      - content
                      - name
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: CentralStatus defines the observed state of Central.
            properties:
              centralStatus:
                description: CentralComponentStatus describes status specific to the
                  central component.
                properties:
                  adminPassword:
                    description: AdminPassword stores information related to the auto-generated
                      admin password.
                    properties:
                      info:
                        description: Info stores information on how to obtain the
                          admin password.
                        type: string
                    type: object
                type: object
              conditions:
                items:
                  description: StackRoxCondition defines a condition for a StackRox
                    custom resource.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      description: ConditionReason is a type of values of condition
                        reason.
                      type: string
                    status:
                      description: ConditionStatus is a type of values of condition
                        status.
                      type: string
                    type:
                      description: ConditionType is a type of values of condition
                        type.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              deployedRelease:
                description: StackRoxRelease describes the Helm "release" that was
                  most recently applied.
                properties:
                  version:
                    type: string
                type: object
            required:
            - conditions
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
