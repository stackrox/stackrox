[tool.poetry]
name = "bundle-helpers"
version = "0.0.1"
description = "StackRox Operator Bundle Helpers"
authors = []

[tool.poetry.dependencies]
python = "^3.6"
pyyaml = "6.0"
pytest = "7.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
