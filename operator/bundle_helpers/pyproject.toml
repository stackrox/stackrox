[tool.poetry]
name = "bundle-helpers"
version = "0.0.1"
description = "Bundle Helpers"
authors = []

[tool.poetry.dependencies]
python = "^3.9"
pyyaml = "6.0.2"
pytest = "7.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
