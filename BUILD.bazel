# gazelle:proto disable
load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    args = [
        "-build_file_name",
        "BUILD.bazel",
    ],
    external = "vendored",
    prefix = "bitbucket.org/stack-rox/apollo",
)
