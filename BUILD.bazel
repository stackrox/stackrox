# gazelle:proto disable_global
load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    args = [
        "-build_file_name",
        "BUILD.bazel",
    ],
    external = "vendored",
    prefix = "github.com/stackrox/rox",
)
