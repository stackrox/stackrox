[allowlist]
  description = "Global Allowlist"

  paths = [
    # Ignore all testdata cert keys.
    '''\/testdata.*\.key$''',
    # Ignore MIGRATION directory (generated roxctl output with dummy certs/keys).
    '''MIGRATION\/''',
  ]
