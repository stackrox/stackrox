---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: riskscoringplugins.config.stackrox.io
spec:
  group: config.stackrox.io
  names:
    kind: RiskScoringPlugin
    listKind: RiskScoringPluginList
    plural: riskscoringplugins
    shortNames:
    - rsp
    singular: riskscoringplugin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.builtin.name
      name: Plugin
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .spec.weight
      name: Weight
      type: number
    - jsonPath: .spec.priority
      name: Priority
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RiskScoringPlugin is the Schema for the riskscoringplugins API.
          It allows configuration of risk scoring plugins in StackRox Central.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RiskScoringPluginSpec defines the desired state of RiskScoringPlugin
            properties:
              builtin:
                description: |-
                  Builtin specifies configuration for a built-in plugin.
                  Required when Type is "builtin".
                properties:
                  name:
                    description: Name references a compiled plugin.
                    enum:
                    - policy-violations
                    - process-baselines
                    - vulnerabilities
                    - service-config
                    - port-exposure
                    - risky-components
                    - component-count
                    - image-age
                    type: string
                  parameters:
                    additionalProperties:
                      type: string
                    description: Parameters are plugin-specific configuration values.
                    type: object
                required:
                - name
                type: object
              enabled:
                default: true
                description: Enabled controls whether this plugin contributes to risk
                  scoring.
                type: boolean
              priority:
                default: 1000
                description: Priority determines execution order. Lower values run
                  earlier.
                format: int32
                type: integer
              type:
                default: builtin
                description: |-
                  Type specifies the plugin execution model.
                  Currently only "builtin" is supported.
                enum:
                - builtin
                type: string
              weight:
                default: "1.0"
                description: |-
                  Weight is a multiplier applied to the plugin's raw score.
                  Specified as a string to avoid float precision issues (e.g., "1.0", "2.5").
                type: string
            required:
            - type
            type: object
          status:
            description: RiskScoringPluginStatus defines the observed state of RiskScoringPlugin
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the plugin's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configId:
                description: ConfigID is the ID assigned by Central for this plugin
                  configuration.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
