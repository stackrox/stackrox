// Code originally generated by pg-bindings generator.

package n2ton3

import (
	"context"

	"github.com/pkg/errors"
	"github.com/stackrox/rox/generated/storage"
	"github.com/stackrox/rox/migrator/migrations"
	frozenSchema "github.com/stackrox/rox/migrator/migrations/frozenschema/v73"
	"github.com/stackrox/rox/migrator/migrations/loghelper"
	legacy "github.com/stackrox/rox/migrator/migrations/n_02_to_n_03_postgres_namespaces/legacy"
	pgStore "github.com/stackrox/rox/migrator/migrations/n_02_to_n_03_postgres_namespaces/postgres"
	"github.com/stackrox/rox/migrator/types"
	pkgMigrations "github.com/stackrox/rox/pkg/migrations"
	"github.com/stackrox/rox/pkg/postgres"
	"github.com/stackrox/rox/pkg/postgres/pgutils"
	"gorm.io/gorm"
)

var (
	startingSeqNum = pkgMigrations.BasePostgresDBVersionSeqNum() + 2 // 113

	migration = types.Migration{
		StartingSeqNum: startingSeqNum,
		VersionAfter:   &storage.Version{SeqNum: int32(startingSeqNum + 1)}, // 114
		Run: func(databases *types.Databases) error {
			legacyStore, err := legacy.New(databases.PkgRocksDB)
			if err != nil {
				return err
			}
			if err := move(databases.DBCtx, databases.GormDB, databases.PostgresDB, legacyStore); err != nil {
				return errors.Wrap(err,
					"moving namespaces from rocksdb to postgres")
			}
			return nil
		},
	}
	batchSize = 10000
	log       = loghelper.LogWrapper{}
)

func move(ctx context.Context, gormDB *gorm.DB, postgresDB postgres.DB, legacyStore legacy.Store) error {
	store := pgStore.New(postgresDB)
	pgutils.CreateTableFromModel(context.Background(), gormDB, frozenSchema.CreateTableNamespacesStmt)

	var namespaces []*storage.NamespaceMetadata
	err := walk(ctx, legacyStore, func(obj *storage.NamespaceMetadata) error {
		namespaces = append(namespaces, obj)
		if len(namespaces) == batchSize {
			if err := store.UpsertMany(ctx, namespaces); err != nil {
				log.WriteToStderrf("failed to persist namespaces to store %v", err)
				return err
			}
			namespaces = namespaces[:0]
		}
		return nil
	})
	if err != nil {
		return err
	}
	if len(namespaces) > 0 {
		if err = store.UpsertMany(ctx, namespaces); err != nil {
			log.WriteToStderrf("failed to persist namespaces to store %v", err)
			return err
		}
	}
	return nil
}

func walk(ctx context.Context, s legacy.Store, fn func(obj *storage.NamespaceMetadata) error) error {
	return s.Walk(ctx, fn)
}

func init() {
	migrations.MustRegisterMigration(migration)
}
