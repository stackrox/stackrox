syntax = "proto3";

package storage;

import "gogoproto/gogo.proto";
import "google/protobuf/timestamp.proto";

option go_package = "storage";

message Usage {
    message SecuredResources {
        int32 nodes = 1;
        int32 cores = 2;
    }

    google.protobuf.Timestamp ts = 1 [(gogoproto.moretags) = 'sql:"pk" search:"Timestamp"'];
    SecuredResources sr = 2;
}
