syntax = "proto3";

option go_package = "storage";
option java_package = "io.stackrox.proto.storage";

import "gogoproto/gogo.proto";

package storage;

message SignatureIntegration {;
    string id   = 1;
    string name = 2;
    string type = 3;

    repeated SignatureVerificationConfig signature_verification_configs = 4;
}

message SignatureVerificationConfig {
    string id = 1;

    oneof config {
        CosignPublicKeyVerification public_key = 2;
    }
}

message CosignPublicKeyVerification {
    message PublicKey {
      string name                   = 1;
      string public_keys_base64_enc = 2;
    }
    repeated PublicKey public_keys = 3;
}
