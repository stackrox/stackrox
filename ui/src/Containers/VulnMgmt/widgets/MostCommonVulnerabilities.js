import React from 'react';
import Widget from 'Components/Widget';

const MostCommonVulnerabilities = () => {
    const header = 'Most Common Vulnerabilities';
    return (
        <Widget className="sx-2 sy-4 pdf-page" header={header}>
            <div />
        </Widget>
    );
};

export default MostCommonVulnerabilities;
