{
    "authProviders": [
        {
            "id": "authProvider-id1",
            "name": "auth-provider-name",
            "type": "oidc",
            "enabled": true,
            "config": {
                "audience": "audience",
                "client_id": "client_id",
                "issuer": "domain"
            },
            "loginUrl": "/auth/response/generic#token=my-token",
            "uiEndpoint": "does not matter",
            "validated": true
        }
    ]
}
