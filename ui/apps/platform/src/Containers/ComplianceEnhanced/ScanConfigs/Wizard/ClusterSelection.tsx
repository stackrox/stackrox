import React, { ReactElement } from 'react';

function ClusterSelection(): ReactElement {
    return (
        <>
            <div>Cluster selection</div>
        </>
    );
}

export default ClusterSelection;
