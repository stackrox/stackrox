import React, { ReactElement } from 'react';

function ProfileSelection(): ReactElement {
    return (
        <>
            <div>Profile selection</div>
        </>
    );
}

export default ProfileSelection;
