apiVersion: v1
items:
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"compliancecheckresults.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ComplianceCheckResult","listKind":"ComplianceCheckResultList","plural":"compliancecheckresults","shortNames":["ccr","checkresults","checkresult"],"singular":"compliancecheckresult"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".status","name":"Status","type":"string"},{"jsonPath":".severity","name":"Severity","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ComplianceCheckResult represent a result of a single compliance \"test\"","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"description":{"description":"A human-readable check description, what and why it does","type":"string"},"id":{"description":"A unique identifier of a check","type":"string"},"instructions":{"description":"How to evaluate if the rule status manually. If no automatic test is present, the rule status will be MANUAL and the administrator should follow these instructions.","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"rationale":{"description":"The rationale of the Rule","type":"string"},"severity":{"description":"The severity of a check status","type":"string"},"status":{"description":"The result of a check","type":"string"},"valuesUsed":{"description":"It stores a list of values used by the check","items":{"type":"string"},"type":"array"},"warnings":{"description":"Any warnings that the user should be aware about.","items":{"type":"string"},"nullable":true,"type":"array"}},"required":["id","severity","status"],"type":"object"}},"served":true,"storage":true,"subresources":{}}]}}
    creationTimestamp: "2024-02-05T10:27:53Z"
    generation: 1
    name: compliancecheckresults.compliance.openshift.io
    resourceVersion: "39248"
    uid: 5e544dc8-e85e-4cfe-a3ce-b2c22ba43ea0
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ComplianceCheckResult
      listKind: ComplianceCheckResultList
      plural: compliancecheckresults
      shortNames:
      - ccr
      - checkresults
      - checkresult
      singular: compliancecheckresult
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .status
        name: Status
        type: string
      - jsonPath: .severity
        name: Severity
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ComplianceCheckResult represent a result of a single compliance
            "test"
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            description:
              description: A human-readable check description, what and why it does
              type: string
            id:
              description: A unique identifier of a check
              type: string
            instructions:
              description: How to evaluate if the rule status manually. If no automatic
                test is present, the rule status will be MANUAL and the administrator
                should follow these instructions.
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            rationale:
              description: The rationale of the Rule
              type: string
            severity:
              description: The severity of a check status
              type: string
            status:
              description: The result of a check
              type: string
            valuesUsed:
              description: It stores a list of values used by the check
              items:
                type: string
              type: array
            warnings:
              description: Any warnings that the user should be aware about.
              items:
                type: string
              nullable: true
              type: array
          required:
          - id
          - severity
          - status
          type: object
      served: true
      storage: true
      subresources: {}
  status:
    acceptedNames:
      kind: ComplianceCheckResult
      listKind: ComplianceCheckResultList
      plural: compliancecheckresults
      shortNames:
      - ccr
      - checkresults
      - checkresult
      singular: compliancecheckresult
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:53Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:54Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"complianceremediations.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ComplianceRemediation","listKind":"ComplianceRemediationList","plural":"complianceremediations","shortNames":["cr","remediations","remediation","rems"],"singular":"complianceremediation"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".status.applicationState","name":"State","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ComplianceRemediation represents a remediation that can be applied to the cluster to fix the found issues.","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"spec":{"description":"Contains the definition of what the remediation should be","properties":{"apply":{"description":"Whether the remediation should be picked up and applied by the operator","type":"boolean"},"current":{"description":"Defines the remediation that is proposed by the scan. If there is no \"outdated\" remediation in this object, the \"current\" remediation is what will be applied.","properties":{"object":{"description":"The remediation payload. This would normally be a full Kubernetes object.","type":"object","x-kubernetes-embedded-resource":true,"x-kubernetes-preserve-unknown-fields":true}},"type":"object"},"outdated":{"description":"In case there was a previous remediation proposed by a previous scan, and that remediation now differs, the old remediation will be kept in this \"outdated\" key. This requires admin intervention to remove this outdated object and ensure the current is what's applied.","properties":{"object":{"description":"The remediation payload. This would normally be a full Kubernetes object.","type":"object","x-kubernetes-embedded-resource":true,"x-kubernetes-preserve-unknown-fields":true}},"type":"object"},"type":{"default":"Configuration","description":"The type of remediation that this object applies. The available types are: Configuration and Enforcement. Where the Configuration type fixes a configuration to match a compliance expectation. The Enforcement type, on the other hand, ensures that the cluster stays in compliance via means of authorization.","enum":["Configuration","Enforcement"],"type":"string"}},"required":["apply"],"type":"object"},"status":{"description":"Contains information on the remediation (whether it's applied or not)","properties":{"applicationState":{"default":"NotApplied","description":"Whether the remediation is already applied or not","type":"string"},"errorMessage":{"type":"string"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    creationTimestamp: "2024-02-05T10:27:54Z"
    generation: 1
    name: complianceremediations.compliance.openshift.io
    resourceVersion: "39252"
    uid: c7d48bf5-e596-4a69-a9c6-a41095e7c766
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ComplianceRemediation
      listKind: ComplianceRemediationList
      plural: complianceremediations
      shortNames:
      - cr
      - remediations
      - remediation
      - rems
      singular: complianceremediation
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .status.applicationState
        name: State
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ComplianceRemediation represents a remediation that can be
            applied to the cluster to fix the found issues.
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: Contains the definition of what the remediation should
                be
              properties:
                apply:
                  description: Whether the remediation should be picked up and applied
                    by the operator
                  type: boolean
                current:
                  description: Defines the remediation that is proposed by the scan.
                    If there is no "outdated" remediation in this object, the "current"
                    remediation is what will be applied.
                  properties:
                    object:
                      description: The remediation payload. This would normally be
                        a full Kubernetes object.
                      type: object
                      x-kubernetes-embedded-resource: true
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                outdated:
                  description: In case there was a previous remediation proposed by
                    a previous scan, and that remediation now differs, the old remediation
                    will be kept in this "outdated" key. This requires admin intervention
                    to remove this outdated object and ensure the current is what's
                    applied.
                  properties:
                    object:
                      description: The remediation payload. This would normally be
                        a full Kubernetes object.
                      type: object
                      x-kubernetes-embedded-resource: true
                      x-kubernetes-preserve-unknown-fields: true
                  type: object
                type:
                  default: Configuration
                  description: 'The type of remediation that this object applies.
                    The available types are: Configuration and Enforcement. Where
                    the Configuration type fixes a configuration to match a compliance
                    expectation. The Enforcement type, on the other hand, ensures
                    that the cluster stays in compliance via means of authorization.'
                  enum:
                  - Configuration
                  - Enforcement
                  type: string
              required:
              - apply
              type: object
            status:
              description: Contains information on the remediation (whether it's applied
                or not)
              properties:
                applicationState:
                  default: NotApplied
                  description: Whether the remediation is already applied or not
                  type: string
                errorMessage:
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: ComplianceRemediation
      listKind: ComplianceRemediationList
      plural: complianceremediations
      shortNames:
      - cr
      - remediations
      - remediation
      - rems
      singular: complianceremediation
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:54Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:54Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"compliancescans.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ComplianceScan","listKind":"ComplianceScanList","plural":"compliancescans","shortNames":["scans","scan"],"singular":"compliancescan"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".status.phase","name":"Phase","type":"string"},{"jsonPath":".status.result","name":"Result","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ComplianceScan represents a scan with a certain configuration that will be applied to objects of a certain entity in the host. These could be nodes that apply to a certain nodeSelector, or the cluster itself.","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"spec":{"description":"The spec is the configuration for the compliance scan.","properties":{"content":{"description":"Is the path to the file that contains the content (the data stream). Note that the path needs to be relative to the `/` (root) directory, as it is in the ContentImage","type":"string"},"contentImage":{"description":"Is the image with the content (Data Stream), that will be used to run OpenSCAP.","type":"string"},"debug":{"description":"Enable debug logging of workloads and OpenSCAP","type":"boolean"},"httpsProxy":{"description":"It is recommended to set the proxy via the config.openshift.io/Proxy object Defines a proxy for the scan to get external resources from. This is useful for disconnected installations with access to a proxy.","type":"string"},"maxRetryOnTimeout":{"default":3,"description":"MaxRetryOnTimeout is the maximum number of times the scan will be retried if it times out.","type":"integer"},"noExternalResources":{"description":"Defines that no external resources in the Data Stream should be used. External resources could be, for instance, CVE feeds. This is useful for disconnected installations without access to a proxy.","type":"boolean"},"nodeSelector":{"additionalProperties":{"type":"string"},"description":"By setting this, it's possible to only run the scan on certain nodes in the cluster. Note that when applying remediations generated from the scan, this should match the selector of the MachineConfigPool you want to apply the remediations to.","type":"object"},"priorityClass":{"description":"Defines the PriorityClass to use for launching scan related pods, the Name of a desired PriorityClass should be set here, this is an optional field, if PriorityClass is invalid or not found, it will be ignored.","type":"string"},"profile":{"description":"Is the profile in the data stream to be used. This is the collection of rules that will be checked for.","type":"string"},"rawResultStorage":{"description":"Specifies settings that pertain to raw result storage.","properties":{"nodeSelector":{"additionalProperties":{"type":"string"},"description":"By setting this, it's possible to configure where the result server instances are run. These instances will mount a Persistent Volume to store the raw results, so special care should be taken to schedule these in trusted nodes.","type":"object"},"pvAccessModes":{"default":["ReadWriteOnce"],"description":"Specifies the access modes that the PersistentVolume will be created with. The persistent volume will hold the raw results of the scan.","items":{"type":"string"},"type":"array"},"rotation":{"default":3,"description":"Specifies the amount of scans for which the raw results will be stored. Older results will get rotated, and it's the responsibility of administrators to store these results elsewhere before rotation happens. Note that a rotation policy of '0' disables rotation entirely. Defaults to 3.","type":"integer"},"size":{"default":"1Gi","description":"Specifies the amount of storage to ask for storing the raw results. Note that if re-scans happen, the new results will also need to be stored. Defaults to 1Gi.","type":"string"},"storageClassName":{"description":"Specifies the StorageClassName to use when creating the PersistentVolumeClaim to hold the raw results. By default this is null, which will attempt to use the default storage class configured in the cluster. If there is no default class specified then this needs to be set.","nullable":true,"type":"string"},"tolerations":{"description":"Specifies tolerations needed for the result server to run on the nodes. This is useful in case the target set of nodes have custom taints that don't allow certain workloads to run. Defaults to allowing scheduling on master nodes.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches the triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"}},"type":"object"},"remediationEnforcement":{"description":"Specifies what to do with remediations of Enforcement type. If left empty, this defaults to \"off\" which doesn't create nor apply any enforcement remediations. If set to \"all\" this creates any enforcement remediations it encounters. Subsequently, this can also be set to a specific type. e.g. setting it to \"gatekeeper\" will apply any enforcement remediations relevant to the Gatekeeper OPA system. These objects will annotated in the content itself with: complianceascode.io/enforcement-type: \u003ctype\u003e","type":"string"},"rule":{"description":"A Rule can be specified if the scan should check only for a specific rule. Note that when leaving this empty, the scan will check for all the rules for a specific profile.","type":"string"},"scanLimits":{"additionalProperties":{"anyOf":[{"type":"integer"},{"type":"string"}],"pattern":"^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$","x-kubernetes-int-or-string":true},"description":"ScanLimits allows to set the resource limits that the scan pods are allowed to use. By default, compliance operator will use sensible defaults (500Mi memory, 100m CPU for the scanner container and 200Mi memory with 100m CPU for the api-resource-collector container).","type":"object"},"scanTolerations":{"default":[{"operator":"Exists"}],"description":"Specifies tolerations needed for the scan to run on the nodes. This is useful in case the target set of nodes have custom taints that don't allow certain workloads to run. Defaults to allowing scheduling on all nodes.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches the triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"},"scanType":{"default":"Node","description":"The type of Compliance scan.","type":"string"},"showNotApplicable":{"default":false,"description":"Determines whether to hide or show results that are not applicable.","type":"boolean"},"strictNodeScan":{"default":true,"description":"Defines whether the scan should proceed if we're not able to scan all the nodes or not. `true` means that the operator should be strict and error out. `false` means that we don't need to be strict and we can proceed.","type":"boolean"},"tailoringConfigMap":{"description":"Is a reference to a ConfigMap that contains the tailoring file. It assumes a key called `tailoring.xml` which will have the tailoring contents.","properties":{"name":{"description":"Name of the ConfigMap being referenced","type":"string"}},"required":["name"],"type":"object"},"timeout":{"default":"30m","description":"Timeout is the maximum amount of time the scan can run. If the scan hasn't finished by then, it will be aborted.","type":"string"}},"type":"object"},"status":{"description":"The status will give valuable information on what's going on with the scan; and, more importantly, if the scan is successful (compliant) or not (non-compliant)","properties":{"conditions":{"description":"Conditions is a set of Condition instances.","items":{"description":"Condition represents an observation of an object's state. Conditions are an extension mechanism intended to be used when the details of an observation are not a priori known or would not apply to all instances of a given Kind. \n Conditions should be added to explicitly convey properties that users and components care about rather than requiring those properties to be inferred from other observations. Once defined, the meaning of a Condition can not be changed arbitrarily - it becomes part of the API, and has the same backwards- and forwards-compatibility concerns of any other part of the API.","properties":{"lastTransitionTime":{"format":"date-time","type":"string"},"message":{"type":"string"},"reason":{"description":"ConditionReason is intended to be a one-word, CamelCase representation of the category of cause of the current status. It is intended to be used in concise output, such as one-line kubectl get output, and in summarizing occurrences of causes.","type":"string"},"status":{"type":"string"},"type":{"description":"ConditionType is the type of the condition and is typically a CamelCased word or short phrase. \n Condition types should indicate state in the \"abnormal-true\" polarity. For example, if the condition indicates when a policy is invalid, the \"is valid\" case is probably the norm, so the condition should be called \"Invalid\".","type":"string"}},"required":["status","type"],"type":"object"},"type":"array"},"currentIndex":{"description":"Specifies the current index of the scan. Given multiple scans, this marks the amount that have been executed.","format":"int64","type":"integer"},"endTimestamp":{"description":"Is the time when the scan was finished","format":"date-time","type":"string"},"errormsg":{"description":"If there are issues on the scan, this will be filled up with an error message.","type":"string"},"phase":{"description":"Is the phase where the scan is at. Normally, one must wait for the scan to reach the phase DONE.","type":"string"},"remainingRetries":{"description":"Is the number of retries left for the scan on timeout","type":"integer"},"result":{"description":"Once the scan reaches the phase DONE, this will contain the result of the scan. Where COMPLIANT means that the scan succeeded; NON-COMPLIANT means that there were rule violations; and ERROR means that the scan couldn't complete due to an issue.","type":"string"},"resultsStorage":{"description":"Specifies the object that's storing the raw results for the scan.","properties":{"apiVersion":{"description":"API version of the referent.","type":"string"},"kind":{"description":"Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"name":{"description":"Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names","type":"string"},"namespace":{"description":"Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/","type":"string"}},"type":"object"},"startTimestamp":{"description":"Is the time when the scan was started","format":"date-time","type":"string"},"warnings":{"description":"If there are warnings on the scan, this will be filled up with warning messages.","type":"string"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    creationTimestamp: "2024-02-05T10:27:54Z"
    generation: 1
    name: compliancescans.compliance.openshift.io
    resourceVersion: "39257"
    uid: e86b8663-f6d8-4efc-94f8-4b162a7dd3c4
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ComplianceScan
      listKind: ComplianceScanList
      plural: compliancescans
      shortNames:
      - scans
      - scan
      singular: compliancescan
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .status.phase
        name: Phase
        type: string
      - jsonPath: .status.result
        name: Result
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ComplianceScan represents a scan with a certain configuration
            that will be applied to objects of a certain entity in the host. These
            could be nodes that apply to a certain nodeSelector, or the cluster itself.
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: The spec is the configuration for the compliance scan.
              properties:
                content:
                  description: Is the path to the file that contains the content (the
                    data stream). Note that the path needs to be relative to the `/`
                    (root) directory, as it is in the ContentImage
                  type: string
                contentImage:
                  description: Is the image with the content (Data Stream), that will
                    be used to run OpenSCAP.
                  type: string
                debug:
                  description: Enable debug logging of workloads and OpenSCAP
                  type: boolean
                httpsProxy:
                  description: It is recommended to set the proxy via the config.openshift.io/Proxy
                    object Defines a proxy for the scan to get external resources
                    from. This is useful for disconnected installations with access
                    to a proxy.
                  type: string
                maxRetryOnTimeout:
                  default: 3
                  description: MaxRetryOnTimeout is the maximum number of times the
                    scan will be retried if it times out.
                  type: integer
                noExternalResources:
                  description: Defines that no external resources in the Data Stream
                    should be used. External resources could be, for instance, CVE
                    feeds. This is useful for disconnected installations without access
                    to a proxy.
                  type: boolean
                nodeSelector:
                  additionalProperties:
                    type: string
                  description: By setting this, it's possible to only run the scan
                    on certain nodes in the cluster. Note that when applying remediations
                    generated from the scan, this should match the selector of the
                    MachineConfigPool you want to apply the remediations to.
                  type: object
                priorityClass:
                  description: Defines the PriorityClass to use for launching scan
                    related pods, the Name of a desired PriorityClass should be set
                    here, this is an optional field, if PriorityClass is invalid or
                    not found, it will be ignored.
                  type: string
                profile:
                  description: Is the profile in the data stream to be used. This
                    is the collection of rules that will be checked for.
                  type: string
                rawResultStorage:
                  description: Specifies settings that pertain to raw result storage.
                  properties:
                    nodeSelector:
                      additionalProperties:
                        type: string
                      description: By setting this, it's possible to configure where
                        the result server instances are run. These instances will
                        mount a Persistent Volume to store the raw results, so special
                        care should be taken to schedule these in trusted nodes.
                      type: object
                    pvAccessModes:
                      default:
                      - ReadWriteOnce
                      description: Specifies the access modes that the PersistentVolume
                        will be created with. The persistent volume will hold the
                        raw results of the scan.
                      items:
                        type: string
                      type: array
                    rotation:
                      default: 3
                      description: Specifies the amount of scans for which the raw
                        results will be stored. Older results will get rotated, and
                        it's the responsibility of administrators to store these results
                        elsewhere before rotation happens. Note that a rotation policy
                        of '0' disables rotation entirely. Defaults to 3.
                      type: integer
                    size:
                      default: 1Gi
                      description: Specifies the amount of storage to ask for storing
                        the raw results. Note that if re-scans happen, the new results
                        will also need to be stored. Defaults to 1Gi.
                      type: string
                    storageClassName:
                      description: Specifies the StorageClassName to use when creating
                        the PersistentVolumeClaim to hold the raw results. By default
                        this is null, which will attempt to use the default storage
                        class configured in the cluster. If there is no default class
                        specified then this needs to be set.
                      nullable: true
                      type: string
                    tolerations:
                      description: Specifies tolerations needed for the result server
                        to run on the nodes. This is useful in case the target set
                        of nodes have custom taints that don't allow certain workloads
                        to run. Defaults to allowing scheduling on master nodes.
                      items:
                        description: The pod this Toleration is attached to tolerates
                          any taint that matches the triple <key,value,effect> using
                          the matching operator <operator>.
                        properties:
                          effect:
                            description: Effect indicates the taint effect to match.
                              Empty means match all taint effects. When specified,
                              allowed values are NoSchedule, PreferNoSchedule and
                              NoExecute.
                            type: string
                          key:
                            description: Key is the taint key that the toleration
                              applies to. Empty means match all taint keys. If the
                              key is empty, operator must be Exists; this combination
                              means to match all values and all keys.
                            type: string
                          operator:
                            description: Operator represents a key's relationship
                              to the value. Valid operators are Exists and Equal.
                              Defaults to Equal. Exists is equivalent to wildcard
                              for value, so that a pod can tolerate all taints of
                              a particular category.
                            type: string
                          tolerationSeconds:
                            description: TolerationSeconds represents the period of
                              time the toleration (which must be of effect NoExecute,
                              otherwise this field is ignored) tolerates the taint.
                              By default, it is not set, which means tolerate the
                              taint forever (do not evict). Zero and negative values
                              will be treated as 0 (evict immediately) by the system.
                            format: int64
                            type: integer
                          value:
                            description: Value is the taint value the toleration matches
                              to. If the operator is Exists, the value should be empty,
                              otherwise just a regular string.
                            type: string
                        type: object
                      type: array
                  type: object
                remediationEnforcement:
                  description: 'Specifies what to do with remediations of Enforcement
                    type. If left empty, this defaults to "off" which doesn''t create
                    nor apply any enforcement remediations. If set to "all" this creates
                    any enforcement remediations it encounters. Subsequently, this
                    can also be set to a specific type. e.g. setting it to "gatekeeper"
                    will apply any enforcement remediations relevant to the Gatekeeper
                    OPA system. These objects will annotated in the content itself
                    with: complianceascode.io/enforcement-type: <type>'
                  type: string
                rule:
                  description: A Rule can be specified if the scan should check only
                    for a specific rule. Note that when leaving this empty, the scan
                    will check for all the rules for a specific profile.
                  type: string
                scanLimits:
                  additionalProperties:
                    anyOf:
                    - type: integer
                    - type: string
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  description: ScanLimits allows to set the resource limits that the
                    scan pods are allowed to use. By default, compliance operator
                    will use sensible defaults (500Mi memory, 100m CPU for the scanner
                    container and 200Mi memory with 100m CPU for the api-resource-collector
                    container).
                  type: object
                scanTolerations:
                  default:
                  - operator: Exists
                  description: Specifies tolerations needed for the scan to run on
                    the nodes. This is useful in case the target set of nodes have
                    custom taints that don't allow certain workloads to run. Defaults
                    to allowing scheduling on all nodes.
                  items:
                    description: The pod this Toleration is attached to tolerates
                      any taint that matches the triple <key,value,effect> using the
                      matching operator <operator>.
                    properties:
                      effect:
                        description: Effect indicates the taint effect to match. Empty
                          means match all taint effects. When specified, allowed values
                          are NoSchedule, PreferNoSchedule and NoExecute.
                        type: string
                      key:
                        description: Key is the taint key that the toleration applies
                          to. Empty means match all taint keys. If the key is empty,
                          operator must be Exists; this combination means to match
                          all values and all keys.
                        type: string
                      operator:
                        description: Operator represents a key's relationship to the
                          value. Valid operators are Exists and Equal. Defaults to
                          Equal. Exists is equivalent to wildcard for value, so that
                          a pod can tolerate all taints of a particular category.
                        type: string
                      tolerationSeconds:
                        description: TolerationSeconds represents the period of time
                          the toleration (which must be of effect NoExecute, otherwise
                          this field is ignored) tolerates the taint. By default,
                          it is not set, which means tolerate the taint forever (do
                          not evict). Zero and negative values will be treated as
                          0 (evict immediately) by the system.
                        format: int64
                        type: integer
                      value:
                        description: Value is the taint value the toleration matches
                          to. If the operator is Exists, the value should be empty,
                          otherwise just a regular string.
                        type: string
                    type: object
                  type: array
                scanType:
                  default: Node
                  description: The type of Compliance scan.
                  type: string
                showNotApplicable:
                  default: false
                  description: Determines whether to hide or show results that are
                    not applicable.
                  type: boolean
                strictNodeScan:
                  default: true
                  description: Defines whether the scan should proceed if we're not
                    able to scan all the nodes or not. `true` means that the operator
                    should be strict and error out. `false` means that we don't need
                    to be strict and we can proceed.
                  type: boolean
                tailoringConfigMap:
                  description: Is a reference to a ConfigMap that contains the tailoring
                    file. It assumes a key called `tailoring.xml` which will have
                    the tailoring contents.
                  properties:
                    name:
                      description: Name of the ConfigMap being referenced
                      type: string
                  required:
                  - name
                  type: object
                timeout:
                  default: 30m
                  description: Timeout is the maximum amount of time the scan can
                    run. If the scan hasn't finished by then, it will be aborted.
                  type: string
              type: object
            status:
              description: The status will give valuable information on what's going
                on with the scan; and, more importantly, if the scan is successful
                (compliant) or not (non-compliant)
              properties:
                conditions:
                  description: Conditions is a set of Condition instances.
                  items:
                    description: "Condition represents an observation of an object's
                      state. Conditions are an extension mechanism intended to be
                      used when the details of an observation are not a priori known
                      or would not apply to all instances of a given Kind. \n Conditions
                      should be added to explicitly convey properties that users and
                      components care about rather than requiring those properties
                      to be inferred from other observations. Once defined, the meaning
                      of a Condition can not be changed arbitrarily - it becomes part
                      of the API, and has the same backwards- and forwards-compatibility
                      concerns of any other part of the API."
                    properties:
                      lastTransitionTime:
                        format: date-time
                        type: string
                      message:
                        type: string
                      reason:
                        description: ConditionReason is intended to be a one-word,
                          CamelCase representation of the category of cause of the
                          current status. It is intended to be used in concise output,
                          such as one-line kubectl get output, and in summarizing
                          occurrences of causes.
                        type: string
                      status:
                        type: string
                      type:
                        description: "ConditionType is the type of the condition and
                          is typically a CamelCased word or short phrase. \n Condition
                          types should indicate state in the \"abnormal-true\" polarity.
                          For example, if the condition indicates when a policy is
                          invalid, the \"is valid\" case is probably the norm, so
                          the condition should be called \"Invalid\"."
                        type: string
                    required:
                    - status
                    - type
                    type: object
                  type: array
                currentIndex:
                  description: Specifies the current index of the scan. Given multiple
                    scans, this marks the amount that have been executed.
                  format: int64
                  type: integer
                endTimestamp:
                  description: Is the time when the scan was finished
                  format: date-time
                  type: string
                errormsg:
                  description: If there are issues on the scan, this will be filled
                    up with an error message.
                  type: string
                phase:
                  description: Is the phase where the scan is at. Normally, one must
                    wait for the scan to reach the phase DONE.
                  type: string
                remainingRetries:
                  description: Is the number of retries left for the scan on timeout
                  type: integer
                result:
                  description: Once the scan reaches the phase DONE, this will contain
                    the result of the scan. Where COMPLIANT means that the scan succeeded;
                    NON-COMPLIANT means that there were rule violations; and ERROR
                    means that the scan couldn't complete due to an issue.
                  type: string
                resultsStorage:
                  description: Specifies the object that's storing the raw results
                    for the scan.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    kind:
                      description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                      type: string
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                      type: string
                    namespace:
                      description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                      type: string
                  type: object
                startTimestamp:
                  description: Is the time when the scan was started
                  format: date-time
                  type: string
                warnings:
                  description: If there are warnings on the scan, this will be filled
                    up with warning messages.
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: ComplianceScan
      listKind: ComplianceScanList
      plural: compliancescans
      shortNames:
      - scans
      - scan
      singular: compliancescan
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:54Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:54Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"compliancesuites.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ComplianceSuite","listKind":"ComplianceSuiteList","plural":"compliancesuites","shortNames":["suites","suite"],"singular":"compliancesuite"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".status.phase","name":"Phase","type":"string"},{"jsonPath":".status.result","name":"Result","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ComplianceSuite represents a set of scans that will be applied to the cluster. These should help deployers achieve a certain compliance target.","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"spec":{"description":"Contains the definition of the suite","properties":{"autoApplyRemediations":{"description":"Defines whether or not the remediations should be applied automatically","type":"boolean"},"autoUpdateRemediations":{"description":"Defines whether or not the remediations should be updated automatically. This is done by deleting the \"outdated\" object from the remediation.","type":"boolean"},"scans":{"description":"Contains a list of the scans to execute on the cluster","items":{"description":"ComplianceScanSpecWrapper provides a ComplianceScanSpec and a Name","properties":{"content":{"description":"Is the path to the file that contains the content (the data stream). Note that the path needs to be relative to the `/` (root) directory, as it is in the ContentImage","type":"string"},"contentImage":{"description":"Is the image with the content (Data Stream), that will be used to run OpenSCAP.","type":"string"},"debug":{"description":"Enable debug logging of workloads and OpenSCAP","type":"boolean"},"httpsProxy":{"description":"It is recommended to set the proxy via the config.openshift.io/Proxy object Defines a proxy for the scan to get external resources from. This is useful for disconnected installations with access to a proxy.","type":"string"},"maxRetryOnTimeout":{"default":3,"description":"MaxRetryOnTimeout is the maximum number of times the scan will be retried if it times out.","type":"integer"},"name":{"description":"Contains a human readable name for the scan. This is to identify the objects that it creates.","type":"string"},"noExternalResources":{"description":"Defines that no external resources in the Data Stream should be used. External resources could be, for instance, CVE feeds. This is useful for disconnected installations without access to a proxy.","type":"boolean"},"nodeSelector":{"additionalProperties":{"type":"string"},"description":"By setting this, it's possible to only run the scan on certain nodes in the cluster. Note that when applying remediations generated from the scan, this should match the selector of the MachineConfigPool you want to apply the remediations to.","type":"object"},"priorityClass":{"description":"Defines the PriorityClass to use for launching scan related pods, the Name of a desired PriorityClass should be set here, this is an optional field, if PriorityClass is invalid or not found, it will be ignored.","type":"string"},"profile":{"description":"Is the profile in the data stream to be used. This is the collection of rules that will be checked for.","type":"string"},"rawResultStorage":{"description":"Specifies settings that pertain to raw result storage.","properties":{"nodeSelector":{"additionalProperties":{"type":"string"},"description":"By setting this, it's possible to configure where the result server instances are run. These instances will mount a Persistent Volume to store the raw results, so special care should be taken to schedule these in trusted nodes.","type":"object"},"pvAccessModes":{"default":["ReadWriteOnce"],"description":"Specifies the access modes that the PersistentVolume will be created with. The persistent volume will hold the raw results of the scan.","items":{"type":"string"},"type":"array"},"rotation":{"default":3,"description":"Specifies the amount of scans for which the raw results will be stored. Older results will get rotated, and it's the responsibility of administrators to store these results elsewhere before rotation happens. Note that a rotation policy of '0' disables rotation entirely. Defaults to 3.","type":"integer"},"size":{"default":"1Gi","description":"Specifies the amount of storage to ask for storing the raw results. Note that if re-scans happen, the new results will also need to be stored. Defaults to 1Gi.","type":"string"},"storageClassName":{"description":"Specifies the StorageClassName to use when creating the PersistentVolumeClaim to hold the raw results. By default this is null, which will attempt to use the default storage class configured in the cluster. If there is no default class specified then this needs to be set.","nullable":true,"type":"string"},"tolerations":{"description":"Specifies tolerations needed for the result server to run on the nodes. This is useful in case the target set of nodes have custom taints that don't allow certain workloads to run. Defaults to allowing scheduling on master nodes.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches the triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"}},"type":"object"},"remediationEnforcement":{"description":"Specifies what to do with remediations of Enforcement type. If left empty, this defaults to \"off\" which doesn't create nor apply any enforcement remediations. If set to \"all\" this creates any enforcement remediations it encounters. Subsequently, this can also be set to a specific type. e.g. setting it to \"gatekeeper\" will apply any enforcement remediations relevant to the Gatekeeper OPA system. These objects will annotated in the content itself with: complianceascode.io/enforcement-type: \u003ctype\u003e","type":"string"},"rule":{"description":"A Rule can be specified if the scan should check only for a specific rule. Note that when leaving this empty, the scan will check for all the rules for a specific profile.","type":"string"},"scanLimits":{"additionalProperties":{"anyOf":[{"type":"integer"},{"type":"string"}],"pattern":"^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$","x-kubernetes-int-or-string":true},"description":"ScanLimits allows to set the resource limits that the scan pods are allowed to use. By default, compliance operator will use sensible defaults (500Mi memory, 100m CPU for the scanner container and 200Mi memory with 100m CPU for the api-resource-collector container).","type":"object"},"scanTolerations":{"default":[{"operator":"Exists"}],"description":"Specifies tolerations needed for the scan to run on the nodes. This is useful in case the target set of nodes have custom taints that don't allow certain workloads to run. Defaults to allowing scheduling on all nodes.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches the triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"},"scanType":{"default":"Node","description":"The type of Compliance scan.","type":"string"},"showNotApplicable":{"default":false,"description":"Determines whether to hide or show results that are not applicable.","type":"boolean"},"strictNodeScan":{"default":true,"description":"Defines whether the scan should proceed if we're not able to scan all the nodes or not. `true` means that the operator should be strict and error out. `false` means that we don't need to be strict and we can proceed.","type":"boolean"},"tailoringConfigMap":{"description":"Is a reference to a ConfigMap that contains the tailoring file. It assumes a key called `tailoring.xml` which will have the tailoring contents.","properties":{"name":{"description":"Name of the ConfigMap being referenced","type":"string"}},"required":["name"],"type":"object"},"timeout":{"default":"30m","description":"Timeout is the maximum amount of time the scan can run. If the scan hasn't finished by then, it will be aborted.","type":"string"}},"type":"object"},"type":"array","x-kubernetes-list-type":"atomic"},"schedule":{"description":"Defines a schedule for the scans to run. This is in cronjob format. Note the scan will still be triggered immediately, and the scheduled scans will start running only after the initial results are ready.","type":"string"},"suspend":{"default":false,"description":"Defines if a schedule should be suspended and is a boolean value, defaulting to False.","type":"boolean"}},"required":["scans"],"type":"object"},"status":{"description":"Contains the current state of the suite","properties":{"conditions":{"description":"Conditions is a set of Condition instances.","items":{"description":"Condition represents an observation of an object's state. Conditions are an extension mechanism intended to be used when the details of an observation are not a priori known or would not apply to all instances of a given Kind. \n Conditions should be added to explicitly convey properties that users and components care about rather than requiring those properties to be inferred from other observations. Once defined, the meaning of a Condition can not be changed arbitrarily - it becomes part of the API, and has the same backwards- and forwards-compatibility concerns of any other part of the API.","properties":{"lastTransitionTime":{"format":"date-time","type":"string"},"message":{"type":"string"},"reason":{"description":"ConditionReason is intended to be a one-word, CamelCase representation of the category of cause of the current status. It is intended to be used in concise output, such as one-line kubectl get output, and in summarizing occurrences of causes.","type":"string"},"status":{"type":"string"},"type":{"description":"ConditionType is the type of the condition and is typically a CamelCased word or short phrase. \n Condition types should indicate state in the \"abnormal-true\" polarity. For example, if the condition indicates when a policy is invalid, the \"is valid\" case is probably the norm, so the condition should be called \"Invalid\".","type":"string"}},"required":["status","type"],"type":"object"},"type":"array"},"errorMessage":{"type":"string"},"phase":{"description":"Represents the status of the compliance scan run.","type":"string"},"result":{"description":"Represents the result of the compliance scan","type":"string"},"scanStatuses":{"items":{"description":"ComplianceScanStatusWrapper provides a ComplianceScanStatus and a Name","properties":{"conditions":{"description":"Conditions is a set of Condition instances.","items":{"description":"Condition represents an observation of an object's state. Conditions are an extension mechanism intended to be used when the details of an observation are not a priori known or would not apply to all instances of a given Kind. \n Conditions should be added to explicitly convey properties that users and components care about rather than requiring those properties to be inferred from other observations. Once defined, the meaning of a Condition can not be changed arbitrarily - it becomes part of the API, and has the same backwards- and forwards-compatibility concerns of any other part of the API.","properties":{"lastTransitionTime":{"format":"date-time","type":"string"},"message":{"type":"string"},"reason":{"description":"ConditionReason is intended to be a one-word, CamelCase representation of the category of cause of the current status. It is intended to be used in concise output, such as one-line kubectl get output, and in summarizing occurrences of causes.","type":"string"},"status":{"type":"string"},"type":{"description":"ConditionType is the type of the condition and is typically a CamelCased word or short phrase. \n Condition types should indicate state in the \"abnormal-true\" polarity. For example, if the condition indicates when a policy is invalid, the \"is valid\" case is probably the norm, so the condition should be called \"Invalid\".","type":"string"}},"required":["status","type"],"type":"object"},"type":"array"},"currentIndex":{"description":"Specifies the current index of the scan. Given multiple scans, this marks the amount that have been executed.","format":"int64","type":"integer"},"endTimestamp":{"description":"Is the time when the scan was finished","format":"date-time","type":"string"},"errormsg":{"description":"If there are issues on the scan, this will be filled up with an error message.","type":"string"},"name":{"description":"Contains a human readable name for the scan. This is to identify the objects that it creates.","type":"string"},"phase":{"description":"Is the phase where the scan is at. Normally, one must wait for the scan to reach the phase DONE.","type":"string"},"remainingRetries":{"description":"Is the number of retries left for the scan on timeout","type":"integer"},"result":{"description":"Once the scan reaches the phase DONE, this will contain the result of the scan. Where COMPLIANT means that the scan succeeded; NON-COMPLIANT means that there were rule violations; and ERROR means that the scan couldn't complete due to an issue.","type":"string"},"resultsStorage":{"description":"Specifies the object that's storing the raw results for the scan.","properties":{"apiVersion":{"description":"API version of the referent.","type":"string"},"kind":{"description":"Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"name":{"description":"Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names","type":"string"},"namespace":{"description":"Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/","type":"string"}},"type":"object"},"startTimestamp":{"description":"Is the time when the scan was started","format":"date-time","type":"string"},"warnings":{"description":"If there are warnings on the scan, this will be filled up with warning messages.","type":"string"}},"type":"object"},"type":"array","x-kubernetes-list-type":"atomic"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    creationTimestamp: "2024-02-05T10:27:54Z"
    generation: 1
    name: compliancesuites.compliance.openshift.io
    resourceVersion: "39262"
    uid: 351b2166-3106-4263-8e87-1fa0873db3bb
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ComplianceSuite
      listKind: ComplianceSuiteList
      plural: compliancesuites
      shortNames:
      - suites
      - suite
      singular: compliancesuite
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .status.phase
        name: Phase
        type: string
      - jsonPath: .status.result
        name: Result
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ComplianceSuite represents a set of scans that will be applied
            to the cluster. These should help deployers achieve a certain compliance
            target.
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: Contains the definition of the suite
              properties:
                autoApplyRemediations:
                  description: Defines whether or not the remediations should be applied
                    automatically
                  type: boolean
                autoUpdateRemediations:
                  description: Defines whether or not the remediations should be updated
                    automatically. This is done by deleting the "outdated" object
                    from the remediation.
                  type: boolean
                scans:
                  description: Contains a list of the scans to execute on the cluster
                  items:
                    description: ComplianceScanSpecWrapper provides a ComplianceScanSpec
                      and a Name
                    properties:
                      content:
                        description: Is the path to the file that contains the content
                          (the data stream). Note that the path needs to be relative
                          to the `/` (root) directory, as it is in the ContentImage
                        type: string
                      contentImage:
                        description: Is the image with the content (Data Stream),
                          that will be used to run OpenSCAP.
                        type: string
                      debug:
                        description: Enable debug logging of workloads and OpenSCAP
                        type: boolean
                      httpsProxy:
                        description: It is recommended to set the proxy via the config.openshift.io/Proxy
                          object Defines a proxy for the scan to get external resources
                          from. This is useful for disconnected installations with
                          access to a proxy.
                        type: string
                      maxRetryOnTimeout:
                        default: 3
                        description: MaxRetryOnTimeout is the maximum number of times
                          the scan will be retried if it times out.
                        type: integer
                      name:
                        description: Contains a human readable name for the scan.
                          This is to identify the objects that it creates.
                        type: string
                      noExternalResources:
                        description: Defines that no external resources in the Data
                          Stream should be used. External resources could be, for
                          instance, CVE feeds. This is useful for disconnected installations
                          without access to a proxy.
                        type: boolean
                      nodeSelector:
                        additionalProperties:
                          type: string
                        description: By setting this, it's possible to only run the
                          scan on certain nodes in the cluster. Note that when applying
                          remediations generated from the scan, this should match
                          the selector of the MachineConfigPool you want to apply
                          the remediations to.
                        type: object
                      priorityClass:
                        description: Defines the PriorityClass to use for launching
                          scan related pods, the Name of a desired PriorityClass should
                          be set here, this is an optional field, if PriorityClass
                          is invalid or not found, it will be ignored.
                        type: string
                      profile:
                        description: Is the profile in the data stream to be used.
                          This is the collection of rules that will be checked for.
                        type: string
                      rawResultStorage:
                        description: Specifies settings that pertain to raw result
                          storage.
                        properties:
                          nodeSelector:
                            additionalProperties:
                              type: string
                            description: By setting this, it's possible to configure
                              where the result server instances are run. These instances
                              will mount a Persistent Volume to store the raw results,
                              so special care should be taken to schedule these in
                              trusted nodes.
                            type: object
                          pvAccessModes:
                            default:
                            - ReadWriteOnce
                            description: Specifies the access modes that the PersistentVolume
                              will be created with. The persistent volume will hold
                              the raw results of the scan.
                            items:
                              type: string
                            type: array
                          rotation:
                            default: 3
                            description: Specifies the amount of scans for which the
                              raw results will be stored. Older results will get rotated,
                              and it's the responsibility of administrators to store
                              these results elsewhere before rotation happens. Note
                              that a rotation policy of '0' disables rotation entirely.
                              Defaults to 3.
                            type: integer
                          size:
                            default: 1Gi
                            description: Specifies the amount of storage to ask for
                              storing the raw results. Note that if re-scans happen,
                              the new results will also need to be stored. Defaults
                              to 1Gi.
                            type: string
                          storageClassName:
                            description: Specifies the StorageClassName to use when
                              creating the PersistentVolumeClaim to hold the raw results.
                              By default this is null, which will attempt to use the
                              default storage class configured in the cluster. If
                              there is no default class specified then this needs
                              to be set.
                            nullable: true
                            type: string
                          tolerations:
                            description: Specifies tolerations needed for the result
                              server to run on the nodes. This is useful in case the
                              target set of nodes have custom taints that don't allow
                              certain workloads to run. Defaults to allowing scheduling
                              on master nodes.
                            items:
                              description: The pod this Toleration is attached to
                                tolerates any taint that matches the triple <key,value,effect>
                                using the matching operator <operator>.
                              properties:
                                effect:
                                  description: Effect indicates the taint effect to
                                    match. Empty means match all taint effects. When
                                    specified, allowed values are NoSchedule, PreferNoSchedule
                                    and NoExecute.
                                  type: string
                                key:
                                  description: Key is the taint key that the toleration
                                    applies to. Empty means match all taint keys.
                                    If the key is empty, operator must be Exists;
                                    this combination means to match all values and
                                    all keys.
                                  type: string
                                operator:
                                  description: Operator represents a key's relationship
                                    to the value. Valid operators are Exists and Equal.
                                    Defaults to Equal. Exists is equivalent to wildcard
                                    for value, so that a pod can tolerate all taints
                                    of a particular category.
                                  type: string
                                tolerationSeconds:
                                  description: TolerationSeconds represents the period
                                    of time the toleration (which must be of effect
                                    NoExecute, otherwise this field is ignored) tolerates
                                    the taint. By default, it is not set, which means
                                    tolerate the taint forever (do not evict). Zero
                                    and negative values will be treated as 0 (evict
                                    immediately) by the system.
                                  format: int64
                                  type: integer
                                value:
                                  description: Value is the taint value the toleration
                                    matches to. If the operator is Exists, the value
                                    should be empty, otherwise just a regular string.
                                  type: string
                              type: object
                            type: array
                        type: object
                      remediationEnforcement:
                        description: 'Specifies what to do with remediations of Enforcement
                          type. If left empty, this defaults to "off" which doesn''t
                          create nor apply any enforcement remediations. If set to
                          "all" this creates any enforcement remediations it encounters.
                          Subsequently, this can also be set to a specific type. e.g.
                          setting it to "gatekeeper" will apply any enforcement remediations
                          relevant to the Gatekeeper OPA system. These objects will
                          annotated in the content itself with: complianceascode.io/enforcement-type:
                          <type>'
                        type: string
                      rule:
                        description: A Rule can be specified if the scan should check
                          only for a specific rule. Note that when leaving this empty,
                          the scan will check for all the rules for a specific profile.
                        type: string
                      scanLimits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: ScanLimits allows to set the resource limits
                          that the scan pods are allowed to use. By default, compliance
                          operator will use sensible defaults (500Mi memory, 100m
                          CPU for the scanner container and 200Mi memory with 100m
                          CPU for the api-resource-collector container).
                        type: object
                      scanTolerations:
                        default:
                        - operator: Exists
                        description: Specifies tolerations needed for the scan to
                          run on the nodes. This is useful in case the target set
                          of nodes have custom taints that don't allow certain workloads
                          to run. Defaults to allowing scheduling on all nodes.
                        items:
                          description: The pod this Toleration is attached to tolerates
                            any taint that matches the triple <key,value,effect> using
                            the matching operator <operator>.
                          properties:
                            effect:
                              description: Effect indicates the taint effect to match.
                                Empty means match all taint effects. When specified,
                                allowed values are NoSchedule, PreferNoSchedule and
                                NoExecute.
                              type: string
                            key:
                              description: Key is the taint key that the toleration
                                applies to. Empty means match all taint keys. If the
                                key is empty, operator must be Exists; this combination
                                means to match all values and all keys.
                              type: string
                            operator:
                              description: Operator represents a key's relationship
                                to the value. Valid operators are Exists and Equal.
                                Defaults to Equal. Exists is equivalent to wildcard
                                for value, so that a pod can tolerate all taints of
                                a particular category.
                              type: string
                            tolerationSeconds:
                              description: TolerationSeconds represents the period
                                of time the toleration (which must be of effect NoExecute,
                                otherwise this field is ignored) tolerates the taint.
                                By default, it is not set, which means tolerate the
                                taint forever (do not evict). Zero and negative values
                                will be treated as 0 (evict immediately) by the system.
                              format: int64
                              type: integer
                            value:
                              description: Value is the taint value the toleration
                                matches to. If the operator is Exists, the value should
                                be empty, otherwise just a regular string.
                              type: string
                          type: object
                        type: array
                      scanType:
                        default: Node
                        description: The type of Compliance scan.
                        type: string
                      showNotApplicable:
                        default: false
                        description: Determines whether to hide or show results that
                          are not applicable.
                        type: boolean
                      strictNodeScan:
                        default: true
                        description: Defines whether the scan should proceed if we're
                          not able to scan all the nodes or not. `true` means that
                          the operator should be strict and error out. `false` means
                          that we don't need to be strict and we can proceed.
                        type: boolean
                      tailoringConfigMap:
                        description: Is a reference to a ConfigMap that contains the
                          tailoring file. It assumes a key called `tailoring.xml`
                          which will have the tailoring contents.
                        properties:
                          name:
                            description: Name of the ConfigMap being referenced
                            type: string
                        required:
                        - name
                        type: object
                      timeout:
                        default: 30m
                        description: Timeout is the maximum amount of time the scan
                          can run. If the scan hasn't finished by then, it will be
                          aborted.
                        type: string
                    type: object
                  type: array
                  x-kubernetes-list-type: atomic
                schedule:
                  description: Defines a schedule for the scans to run. This is in
                    cronjob format. Note the scan will still be triggered immediately,
                    and the scheduled scans will start running only after the initial
                    results are ready.
                  type: string
                suspend:
                  default: false
                  description: Defines if a schedule should be suspended and is a
                    boolean value, defaulting to False.
                  type: boolean
              required:
              - scans
              type: object
            status:
              description: Contains the current state of the suite
              properties:
                conditions:
                  description: Conditions is a set of Condition instances.
                  items:
                    description: "Condition represents an observation of an object's
                      state. Conditions are an extension mechanism intended to be
                      used when the details of an observation are not a priori known
                      or would not apply to all instances of a given Kind. \n Conditions
                      should be added to explicitly convey properties that users and
                      components care about rather than requiring those properties
                      to be inferred from other observations. Once defined, the meaning
                      of a Condition can not be changed arbitrarily - it becomes part
                      of the API, and has the same backwards- and forwards-compatibility
                      concerns of any other part of the API."
                    properties:
                      lastTransitionTime:
                        format: date-time
                        type: string
                      message:
                        type: string
                      reason:
                        description: ConditionReason is intended to be a one-word,
                          CamelCase representation of the category of cause of the
                          current status. It is intended to be used in concise output,
                          such as one-line kubectl get output, and in summarizing
                          occurrences of causes.
                        type: string
                      status:
                        type: string
                      type:
                        description: "ConditionType is the type of the condition and
                          is typically a CamelCased word or short phrase. \n Condition
                          types should indicate state in the \"abnormal-true\" polarity.
                          For example, if the condition indicates when a policy is
                          invalid, the \"is valid\" case is probably the norm, so
                          the condition should be called \"Invalid\"."
                        type: string
                    required:
                    - status
                    - type
                    type: object
                  type: array
                errorMessage:
                  type: string
                phase:
                  description: Represents the status of the compliance scan run.
                  type: string
                result:
                  description: Represents the result of the compliance scan
                  type: string
                scanStatuses:
                  items:
                    description: ComplianceScanStatusWrapper provides a ComplianceScanStatus
                      and a Name
                    properties:
                      conditions:
                        description: Conditions is a set of Condition instances.
                        items:
                          description: "Condition represents an observation of an
                            object's state. Conditions are an extension mechanism
                            intended to be used when the details of an observation
                            are not a priori known or would not apply to all instances
                            of a given Kind. \n Conditions should be added to explicitly
                            convey properties that users and components care about
                            rather than requiring those properties to be inferred
                            from other observations. Once defined, the meaning of
                            a Condition can not be changed arbitrarily - it becomes
                            part of the API, and has the same backwards- and forwards-compatibility
                            concerns of any other part of the API."
                          properties:
                            lastTransitionTime:
                              format: date-time
                              type: string
                            message:
                              type: string
                            reason:
                              description: ConditionReason is intended to be a one-word,
                                CamelCase representation of the category of cause
                                of the current status. It is intended to be used in
                                concise output, such as one-line kubectl get output,
                                and in summarizing occurrences of causes.
                              type: string
                            status:
                              type: string
                            type:
                              description: "ConditionType is the type of the condition
                                and is typically a CamelCased word or short phrase.
                                \n Condition types should indicate state in the \"abnormal-true\"
                                polarity. For example, if the condition indicates
                                when a policy is invalid, the \"is valid\" case is
                                probably the norm, so the condition should be called
                                \"Invalid\"."
                              type: string
                          required:
                          - status
                          - type
                          type: object
                        type: array
                      currentIndex:
                        description: Specifies the current index of the scan. Given
                          multiple scans, this marks the amount that have been executed.
                        format: int64
                        type: integer
                      endTimestamp:
                        description: Is the time when the scan was finished
                        format: date-time
                        type: string
                      errormsg:
                        description: If there are issues on the scan, this will be
                          filled up with an error message.
                        type: string
                      name:
                        description: Contains a human readable name for the scan.
                          This is to identify the objects that it creates.
                        type: string
                      phase:
                        description: Is the phase where the scan is at. Normally,
                          one must wait for the scan to reach the phase DONE.
                        type: string
                      remainingRetries:
                        description: Is the number of retries left for the scan on
                          timeout
                        type: integer
                      result:
                        description: Once the scan reaches the phase DONE, this will
                          contain the result of the scan. Where COMPLIANT means that
                          the scan succeeded; NON-COMPLIANT means that there were
                          rule violations; and ERROR means that the scan couldn't
                          complete due to an issue.
                        type: string
                      resultsStorage:
                        description: Specifies the object that's storing the raw results
                          for the scan.
                        properties:
                          apiVersion:
                            description: API version of the referent.
                            type: string
                          kind:
                            description: 'Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                            type: string
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names'
                            type: string
                          namespace:
                            description: 'Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/'
                            type: string
                        type: object
                      startTimestamp:
                        description: Is the time when the scan was started
                        format: date-time
                        type: string
                      warnings:
                        description: If there are warnings on the scan, this will
                          be filled up with warning messages.
                        type: string
                    type: object
                  type: array
                  x-kubernetes-list-type: atomic
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: ComplianceSuite
      listKind: ComplianceSuiteList
      plural: compliancesuites
      shortNames:
      - suites
      - suite
      singular: compliancesuite
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"profilebundles.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ProfileBundle","listKind":"ProfileBundleList","plural":"profilebundles","shortNames":["pb"],"singular":"profilebundle"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".spec.contentImage","name":"ContentImage","type":"string"},{"jsonPath":".spec.contentFile","name":"ContentFile","type":"string"},{"jsonPath":".status.dataStreamStatus","name":"Status","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ProfileBundle is the Schema for the profilebundles API","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"spec":{"description":"Defines the desired state of ProfileBundle","properties":{"contentFile":{"description":"Is the path for the file in the image that contains the content for this bundle.","type":"string"},"contentImage":{"description":"Is the path for the image that contains the content for this bundle.","type":"string"}},"required":["contentFile","contentImage"],"type":"object"},"status":{"description":"Defines the observed state of ProfileBundle","properties":{"conditions":{"description":"Defines the conditions for the ProfileBundle. Valid conditions are: - Ready: Indicates if the ProfileBundle is Ready parsing or not.","items":{"description":"Condition represents an observation of an object's state. Conditions are an extension mechanism intended to be used when the details of an observation are not a priori known or would not apply to all instances of a given Kind. \n Conditions should be added to explicitly convey properties that users and components care about rather than requiring those properties to be inferred from other observations. Once defined, the meaning of a Condition can not be changed arbitrarily - it becomes part of the API, and has the same backwards- and forwards-compatibility concerns of any other part of the API.","properties":{"lastTransitionTime":{"format":"date-time","type":"string"},"message":{"type":"string"},"reason":{"description":"ConditionReason is intended to be a one-word, CamelCase representation of the category of cause of the current status. It is intended to be used in concise output, such as one-line kubectl get output, and in summarizing occurrences of causes.","type":"string"},"status":{"type":"string"},"type":{"description":"ConditionType is the type of the condition and is typically a CamelCased word or short phrase. \n Condition types should indicate state in the \"abnormal-true\" polarity. For example, if the condition indicates when a policy is invalid, the \"is valid\" case is probably the norm, so the condition should be called \"Invalid\".","type":"string"}},"required":["status","type"],"type":"object"},"type":"array"},"dataStreamStatus":{"default":"PENDING","description":"Presents the current status for the datastream for this bundle","type":"string"},"errorMessage":{"description":"If there's an error in the datastream, it'll be presented here","type":"string"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    creationTimestamp: "2024-02-05T10:27:55Z"
    generation: 1
    name: profilebundles.compliance.openshift.io
    resourceVersion: "39266"
    uid: 48fab3e3-4107-4231-99f3-80b0b7b9f36b
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ProfileBundle
      listKind: ProfileBundleList
      plural: profilebundles
      shortNames:
      - pb
      singular: profilebundle
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .spec.contentImage
        name: ContentImage
        type: string
      - jsonPath: .spec.contentFile
        name: ContentFile
        type: string
      - jsonPath: .status.dataStreamStatus
        name: Status
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ProfileBundle is the Schema for the profilebundles API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: Defines the desired state of ProfileBundle
              properties:
                contentFile:
                  description: Is the path for the file in the image that contains
                    the content for this bundle.
                  type: string
                contentImage:
                  description: Is the path for the image that contains the content
                    for this bundle.
                  type: string
              required:
              - contentFile
              - contentImage
              type: object
            status:
              description: Defines the observed state of ProfileBundle
              properties:
                conditions:
                  description: 'Defines the conditions for the ProfileBundle. Valid
                    conditions are: - Ready: Indicates if the ProfileBundle is Ready
                    parsing or not.'
                  items:
                    description: "Condition represents an observation of an object's
                      state. Conditions are an extension mechanism intended to be
                      used when the details of an observation are not a priori known
                      or would not apply to all instances of a given Kind. \n Conditions
                      should be added to explicitly convey properties that users and
                      components care about rather than requiring those properties
                      to be inferred from other observations. Once defined, the meaning
                      of a Condition can not be changed arbitrarily - it becomes part
                      of the API, and has the same backwards- and forwards-compatibility
                      concerns of any other part of the API."
                    properties:
                      lastTransitionTime:
                        format: date-time
                        type: string
                      message:
                        type: string
                      reason:
                        description: ConditionReason is intended to be a one-word,
                          CamelCase representation of the category of cause of the
                          current status. It is intended to be used in concise output,
                          such as one-line kubectl get output, and in summarizing
                          occurrences of causes.
                        type: string
                      status:
                        type: string
                      type:
                        description: "ConditionType is the type of the condition and
                          is typically a CamelCased word or short phrase. \n Condition
                          types should indicate state in the \"abnormal-true\" polarity.
                          For example, if the condition indicates when a policy is
                          invalid, the \"is valid\" case is probably the norm, so
                          the condition should be called \"Invalid\"."
                        type: string
                    required:
                    - status
                    - type
                    type: object
                  type: array
                dataStreamStatus:
                  default: PENDING
                  description: Presents the current status for the datastream for
                    this bundle
                  type: string
                errorMessage:
                  description: If there's an error in the datastream, it'll be presented
                    here
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: ProfileBundle
      listKind: ProfileBundleList
      plural: profilebundles
      shortNames:
      - pb
      singular: profilebundle
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"profiles.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"Profile","listKind":"ProfileList","plural":"profiles","shortNames":["profs","prof"],"singular":"profile"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".metadata.creationTimestamp","name":"Age","type":"date"},{"jsonPath":".version","name":"Version","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"Profile is the Schema for the profiles API","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"description":{"type":"string"},"id":{"type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"rules":{"items":{"description":"ProfileRule defines the name of a specific rule in the profile","type":"string"},"nullable":true,"type":"array","x-kubernetes-list-type":"atomic"},"title":{"type":"string"},"values":{"items":{"description":"ProfileValue defines a value for a setting in the profile","type":"string"},"nullable":true,"type":"array","x-kubernetes-list-type":"atomic"},"version":{"type":"string"}},"required":["description","id","title"],"type":"object"}},"served":true,"storage":true,"subresources":{}}]}}
    creationTimestamp: "2024-02-05T10:27:55Z"
    generation: 1
    name: profiles.compliance.openshift.io
    resourceVersion: "39269"
    uid: bca65960-f421-4bf6-9e6d-94db3aea3ad1
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: Profile
      listKind: ProfileList
      plural: profiles
      shortNames:
      - profs
      - prof
      singular: profile
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .metadata.creationTimestamp
        name: Age
        type: date
      - jsonPath: .version
        name: Version
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Profile is the Schema for the profiles API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            description:
              type: string
            id:
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            rules:
              items:
                description: ProfileRule defines the name of a specific rule in the
                  profile
                type: string
              nullable: true
              type: array
              x-kubernetes-list-type: atomic
            title:
              type: string
            values:
              items:
                description: ProfileValue defines a value for a setting in the profile
                type: string
              nullable: true
              type: array
              x-kubernetes-list-type: atomic
            version:
              type: string
          required:
          - description
          - id
          - title
          type: object
      served: true
      storage: true
      subresources: {}
  status:
    acceptedNames:
      kind: Profile
      listKind: ProfileList
      plural: profiles
      shortNames:
      - profs
      - prof
      singular: profile
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"rules.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"Rule","listKind":"RuleList","plural":"rules","singular":"rule"},"scope":"Namespaced","versions":[{"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"Rule is the Schema for the rules API","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"availableFixes":{"description":"The Available fixes","items":{"description":"FixDefinition Specifies a fix or remediation that applies to a rule","properties":{"disruption":{"description":"An estimate of the potential disruption or operational degradation that this fix will impose in the target system","type":"string"},"fixObject":{"description":"an object that should bring the rule into compliance","type":"object","x-kubernetes-embedded-resource":true,"x-kubernetes-preserve-unknown-fields":true},"platform":{"description":"The platform that the fix applies to","type":"string"}},"type":"object"},"nullable":true,"type":"array","x-kubernetes-list-type":"atomic"},"checkType":{"description":"What type of check will this rule execute: Platform, Node or none (represented by an empty string)","type":"string"},"description":{"description":"The description of the Rule","type":"string"},"id":{"description":"The XCCDF ID","type":"string"},"instructions":{"description":"Instructions for auditing this specific rule","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"rationale":{"description":"The rationale of the Rule","type":"string"},"severity":{"description":"The severity level","type":"string"},"title":{"description":"The title of the Rule","type":"string"},"warning":{"description":"A discretionary warning about the of the Rule","type":"string"}},"required":["id","title"],"type":"object"}},"served":true,"storage":true}]}}
    creationTimestamp: "2024-02-05T10:27:55Z"
    generation: 1
    name: rules.compliance.openshift.io
    resourceVersion: "39272"
    uid: abaf6dfc-fb79-49dd-ac02-867a5d65ce5d
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: Rule
      listKind: RuleList
      plural: rules
      singular: rule
    scope: Namespaced
    versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Rule is the Schema for the rules API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            availableFixes:
              description: The Available fixes
              items:
                description: FixDefinition Specifies a fix or remediation that applies
                  to a rule
                properties:
                  disruption:
                    description: An estimate of the potential disruption or operational
                      degradation that this fix will impose in the target system
                    type: string
                  fixObject:
                    description: an object that should bring the rule into compliance
                    type: object
                    x-kubernetes-embedded-resource: true
                    x-kubernetes-preserve-unknown-fields: true
                  platform:
                    description: The platform that the fix applies to
                    type: string
                type: object
              nullable: true
              type: array
              x-kubernetes-list-type: atomic
            checkType:
              description: 'What type of check will this rule execute: Platform, Node
                or none (represented by an empty string)'
              type: string
            description:
              description: The description of the Rule
              type: string
            id:
              description: The XCCDF ID
              type: string
            instructions:
              description: Instructions for auditing this specific rule
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            rationale:
              description: The rationale of the Rule
              type: string
            severity:
              description: The severity level
              type: string
            title:
              description: The title of the Rule
              type: string
            warning:
              description: A discretionary warning about the of the Rule
              type: string
          required:
          - id
          - title
          type: object
      served: true
      storage: true
  status:
    acceptedNames:
      kind: Rule
      listKind: RuleList
      plural: rules
      singular: rule
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:55Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"scansettingbindings.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ScanSettingBinding","listKind":"ScanSettingBindingList","plural":"scansettingbindings","shortNames":["ssb"],"singular":"scansettingbinding"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"jsonPath":".status.phase","name":"Status","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ScanSettingBinding is the Schema for the scansettingbindings API","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"profiles":{"items":{"properties":{"apiGroup":{"type":"string"},"kind":{"type":"string"},"name":{"type":"string"}},"type":"object"},"type":"array"},"settingsRef":{"default":{"apiGroup":"compliance.openshift.io/v1alpha1","kind":"ScanSetting","name":"default"},"properties":{"apiGroup":{"type":"string"},"kind":{"type":"string"},"name":{"type":"string"}},"type":"object"},"spec":{"description":"This is a dummy spec to accommodate https://github.com/operator-framework/operator-sdk/issues/5584","type":"object"},"status":{"properties":{"conditions":{"description":"Conditions is a set of Condition instances.","items":{"description":"Condition represents an observation of an object's state. Conditions are an extension mechanism intended to be used when the details of an observation are not a priori known or would not apply to all instances of a given Kind. \n Conditions should be added to explicitly convey properties that users and components care about rather than requiring those properties to be inferred from other observations. Once defined, the meaning of a Condition can not be changed arbitrarily - it becomes part of the API, and has the same backwards- and forwards-compatibility concerns of any other part of the API.","properties":{"lastTransitionTime":{"format":"date-time","type":"string"},"message":{"type":"string"},"reason":{"description":"ConditionReason is intended to be a one-word, CamelCase representation of the category of cause of the current status. It is intended to be used in concise output, such as one-line kubectl get output, and in summarizing occurrences of causes.","type":"string"},"status":{"type":"string"},"type":{"description":"ConditionType is the type of the condition and is typically a CamelCased word or short phrase. \n Condition types should indicate state in the \"abnormal-true\" polarity. For example, if the condition indicates when a policy is invalid, the \"is valid\" case is probably the norm, so the condition should be called \"Invalid\".","type":"string"}},"required":["status","type"],"type":"object"},"type":"array"},"outputRef":{"description":"Reference to the object generated from this ScanSettingBinding","nullable":true,"properties":{"apiGroup":{"description":"APIGroup is the group for the resource being referenced. If APIGroup is not specified, the specified Kind must be in the core API group. For any other third-party types, APIGroup is required.","type":"string"},"kind":{"description":"Kind is the type of resource being referenced","type":"string"},"name":{"description":"Name is the name of resource being referenced","type":"string"}},"required":["kind","name"],"type":"object","x-kubernetes-map-type":"atomic"},"phase":{"type":"string"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    generation: 1
    name: scansettingbindings.compliance.openshift.io
    resourceVersion: "39278"
    uid: e1893ed3-d0a2-4f2e-8ffa-25b18d4b80c1
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ScanSettingBinding
      listKind: ScanSettingBindingList
      plural: scansettingbindings
      shortNames:
      - ssb
      singular: scansettingbinding
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - jsonPath: .status.phase
        name: Status
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ScanSettingBinding is the Schema for the scansettingbindings
            API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            profiles:
              items:
                properties:
                  apiGroup:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                type: object
              type: array
            settingsRef:
              default:
                apiGroup: compliance.openshift.io/v1alpha1
                kind: ScanSetting
                name: default
              properties:
                apiGroup:
                  type: string
                kind:
                  type: string
                name:
                  type: string
              type: object
            spec:
              description: This is a dummy spec to accommodate https://github.com/operator-framework/operator-sdk/issues/5584
              type: object
            status:
              properties:
                conditions:
                  description: Conditions is a set of Condition instances.
                  items:
                    description: "Condition represents an observation of an object's
                      state. Conditions are an extension mechanism intended to be
                      used when the details of an observation are not a priori known
                      or would not apply to all instances of a given Kind. \n Conditions
                      should be added to explicitly convey properties that users and
                      components care about rather than requiring those properties
                      to be inferred from other observations. Once defined, the meaning
                      of a Condition can not be changed arbitrarily - it becomes part
                      of the API, and has the same backwards- and forwards-compatibility
                      concerns of any other part of the API."
                    properties:
                      lastTransitionTime:
                        format: date-time
                        type: string
                      message:
                        type: string
                      reason:
                        description: ConditionReason is intended to be a one-word,
                          CamelCase representation of the category of cause of the
                          current status. It is intended to be used in concise output,
                          such as one-line kubectl get output, and in summarizing
                          occurrences of causes.
                        type: string
                      status:
                        type: string
                      type:
                        description: "ConditionType is the type of the condition and
                          is typically a CamelCased word or short phrase. \n Condition
                          types should indicate state in the \"abnormal-true\" polarity.
                          For example, if the condition indicates when a policy is
                          invalid, the \"is valid\" case is probably the norm, so
                          the condition should be called \"Invalid\"."
                        type: string
                    required:
                    - status
                    - type
                    type: object
                  type: array
                outputRef:
                  description: Reference to the object generated from this ScanSettingBinding
                  nullable: true
                  properties:
                    apiGroup:
                      description: APIGroup is the group for the resource being referenced.
                        If APIGroup is not specified, the specified Kind must be in
                        the core API group. For any other third-party types, APIGroup
                        is required.
                      type: string
                    kind:
                      description: Kind is the type of resource being referenced
                      type: string
                    name:
                      description: Name is the name of resource being referenced
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                phase:
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: ScanSettingBinding
      listKind: ScanSettingBindingList
      plural: scansettingbindings
      shortNames:
      - ssb
      singular: scansettingbinding
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"scansettings.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"ScanSetting","listKind":"ScanSettingList","plural":"scansettings","shortNames":["ss"],"singular":"scansetting"},"scope":"Namespaced","versions":[{"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"ScanSetting is the Schema for the scansettings API","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"autoApplyRemediations":{"description":"Defines whether or not the remediations should be applied automatically","type":"boolean"},"autoUpdateRemediations":{"description":"Defines whether or not the remediations should be updated automatically. This is done by deleting the \"outdated\" object from the remediation.","type":"boolean"},"debug":{"description":"Enable debug logging of workloads and OpenSCAP","type":"boolean"},"httpsProxy":{"description":"It is recommended to set the proxy via the config.openshift.io/Proxy object Defines a proxy for the scan to get external resources from. This is useful for disconnected installations with access to a proxy.","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"maxRetryOnTimeout":{"default":3,"description":"MaxRetryOnTimeout is the maximum number of times the scan will be retried if it times out.","type":"integer"},"metadata":{"type":"object"},"noExternalResources":{"description":"Defines that no external resources in the Data Stream should be used. External resources could be, for instance, CVE feeds. This is useful for disconnected installations without access to a proxy.","type":"boolean"},"priorityClass":{"description":"Defines the PriorityClass to use for launching scan related pods, the Name of a desired PriorityClass should be set here, this is an optional field, if PriorityClass is invalid or not found, it will be ignored.","type":"string"},"rawResultStorage":{"description":"Specifies settings that pertain to raw result storage.","properties":{"nodeSelector":{"additionalProperties":{"type":"string"},"description":"By setting this, it's possible to configure where the result server instances are run. These instances will mount a Persistent Volume to store the raw results, so special care should be taken to schedule these in trusted nodes.","type":"object"},"pvAccessModes":{"default":["ReadWriteOnce"],"description":"Specifies the access modes that the PersistentVolume will be created with. The persistent volume will hold the raw results of the scan.","items":{"type":"string"},"type":"array"},"rotation":{"default":3,"description":"Specifies the amount of scans for which the raw results will be stored. Older results will get rotated, and it's the responsibility of administrators to store these results elsewhere before rotation happens. Note that a rotation policy of '0' disables rotation entirely. Defaults to 3.","type":"integer"},"size":{"default":"1Gi","description":"Specifies the amount of storage to ask for storing the raw results. Note that if re-scans happen, the new results will also need to be stored. Defaults to 1Gi.","type":"string"},"storageClassName":{"description":"Specifies the StorageClassName to use when creating the PersistentVolumeClaim to hold the raw results. By default this is null, which will attempt to use the default storage class configured in the cluster. If there is no default class specified then this needs to be set.","nullable":true,"type":"string"},"tolerations":{"description":"Specifies tolerations needed for the result server to run on the nodes. This is useful in case the target set of nodes have custom taints that don't allow certain workloads to run. Defaults to allowing scheduling on master nodes.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches the triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"}},"type":"object"},"remediationEnforcement":{"description":"Specifies what to do with remediations of Enforcement type. If left empty, this defaults to \"off\" which doesn't create nor apply any enforcement remediations. If set to \"all\" this creates any enforcement remediations it encounters. Subsequently, this can also be set to a specific type. e.g. setting it to \"gatekeeper\" will apply any enforcement remediations relevant to the Gatekeeper OPA system. These objects will annotated in the content itself with: complianceascode.io/enforcement-type: \u003ctype\u003e","type":"string"},"roles":{"description":"The list of roles to apply node-specific checks to. \n This will be translated to the standard Kubernetes role label `node-role.kubernetes.io/\u003crole name\u003e`. \n It's also possible to specify `@all` as a role, which will run a scan on all nodes by not specifying a node selector as we normally do. The usage of `@all` in OpenShift is discouraged as the operator won't be able to apply remediations unless roles are specified. \n Note that tolerations must still be configured for the opeartor to appropriately schedule scans.","items":{"type":"string"},"type":"array"},"scanLimits":{"additionalProperties":{"anyOf":[{"type":"integer"},{"type":"string"}],"pattern":"^(\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\\+|-)?(([0-9]+(\\.[0-9]*)?)|(\\.[0-9]+))))?$","x-kubernetes-int-or-string":true},"description":"ScanLimits allows to set the resource limits that the scan pods are allowed to use. By default, compliance operator will use sensible defaults (500Mi memory, 100m CPU for the scanner container and 200Mi memory with 100m CPU for the api-resource-collector container).","type":"object"},"scanTolerations":{"default":[{"operator":"Exists"}],"description":"Specifies tolerations needed for the scan to run on the nodes. This is useful in case the target set of nodes have custom taints that don't allow certain workloads to run. Defaults to allowing scheduling on all nodes.","items":{"description":"The pod this Toleration is attached to tolerates any taint that matches the triple \u003ckey,value,effect\u003e using the matching operator \u003coperator\u003e.","properties":{"effect":{"description":"Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.","type":"string"},"key":{"description":"Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.","type":"string"},"operator":{"description":"Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.","type":"string"},"tolerationSeconds":{"description":"TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.","format":"int64","type":"integer"},"value":{"description":"Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.","type":"string"}},"type":"object"},"type":"array"},"schedule":{"description":"Defines a schedule for the scans to run. This is in cronjob format. Note the scan will still be triggered immediately, and the scheduled scans will start running only after the initial results are ready.","type":"string"},"showNotApplicable":{"default":false,"description":"Determines whether to hide or show results that are not applicable.","type":"boolean"},"strictNodeScan":{"default":true,"description":"Defines whether the scan should proceed if we're not able to scan all the nodes or not. `true` means that the operator should be strict and error out. `false` means that we don't need to be strict and we can proceed.","type":"boolean"},"suspend":{"default":false,"description":"Defines if a schedule should be suspended and is a boolean value, defaulting to False.","type":"boolean"},"timeout":{"default":"30m","description":"Timeout is the maximum amount of time the scan can run. If the scan hasn't finished by then, it will be aborted.","type":"string"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    creationTimestamp: "2024-02-05T10:27:56Z"
    generation: 1
    name: scansettings.compliance.openshift.io
    resourceVersion: "39281"
    uid: b39d1435-756e-455a-8433-6e0cc4e512b9
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: ScanSetting
      listKind: ScanSettingList
      plural: scansettings
      shortNames:
      - ss
      singular: scansetting
    scope: Namespaced
    versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: ScanSetting is the Schema for the scansettings API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            autoApplyRemediations:
              description: Defines whether or not the remediations should be applied
                automatically
              type: boolean
            autoUpdateRemediations:
              description: Defines whether or not the remediations should be updated
                automatically. This is done by deleting the "outdated" object from
                the remediation.
              type: boolean
            debug:
              description: Enable debug logging of workloads and OpenSCAP
              type: boolean
            httpsProxy:
              description: It is recommended to set the proxy via the config.openshift.io/Proxy
                object Defines a proxy for the scan to get external resources from.
                This is useful for disconnected installations with access to a proxy.
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            maxRetryOnTimeout:
              default: 3
              description: MaxRetryOnTimeout is the maximum number of times the scan
                will be retried if it times out.
              type: integer
            metadata:
              type: object
            noExternalResources:
              description: Defines that no external resources in the Data Stream should
                be used. External resources could be, for instance, CVE feeds. This
                is useful for disconnected installations without access to a proxy.
              type: boolean
            priorityClass:
              description: Defines the PriorityClass to use for launching scan related
                pods, the Name of a desired PriorityClass should be set here, this
                is an optional field, if PriorityClass is invalid or not found, it
                will be ignored.
              type: string
            rawResultStorage:
              description: Specifies settings that pertain to raw result storage.
              properties:
                nodeSelector:
                  additionalProperties:
                    type: string
                  description: By setting this, it's possible to configure where the
                    result server instances are run. These instances will mount a
                    Persistent Volume to store the raw results, so special care should
                    be taken to schedule these in trusted nodes.
                  type: object
                pvAccessModes:
                  default:
                  - ReadWriteOnce
                  description: Specifies the access modes that the PersistentVolume
                    will be created with. The persistent volume will hold the raw
                    results of the scan.
                  items:
                    type: string
                  type: array
                rotation:
                  default: 3
                  description: Specifies the amount of scans for which the raw results
                    will be stored. Older results will get rotated, and it's the responsibility
                    of administrators to store these results elsewhere before rotation
                    happens. Note that a rotation policy of '0' disables rotation
                    entirely. Defaults to 3.
                  type: integer
                size:
                  default: 1Gi
                  description: Specifies the amount of storage to ask for storing
                    the raw results. Note that if re-scans happen, the new results
                    will also need to be stored. Defaults to 1Gi.
                  type: string
                storageClassName:
                  description: Specifies the StorageClassName to use when creating
                    the PersistentVolumeClaim to hold the raw results. By default
                    this is null, which will attempt to use the default storage class
                    configured in the cluster. If there is no default class specified
                    then this needs to be set.
                  nullable: true
                  type: string
                tolerations:
                  description: Specifies tolerations needed for the result server
                    to run on the nodes. This is useful in case the target set of
                    nodes have custom taints that don't allow certain workloads to
                    run. Defaults to allowing scheduling on master nodes.
                  items:
                    description: The pod this Toleration is attached to tolerates
                      any taint that matches the triple <key,value,effect> using the
                      matching operator <operator>.
                    properties:
                      effect:
                        description: Effect indicates the taint effect to match. Empty
                          means match all taint effects. When specified, allowed values
                          are NoSchedule, PreferNoSchedule and NoExecute.
                        type: string
                      key:
                        description: Key is the taint key that the toleration applies
                          to. Empty means match all taint keys. If the key is empty,
                          operator must be Exists; this combination means to match
                          all values and all keys.
                        type: string
                      operator:
                        description: Operator represents a key's relationship to the
                          value. Valid operators are Exists and Equal. Defaults to
                          Equal. Exists is equivalent to wildcard for value, so that
                          a pod can tolerate all taints of a particular category.
                        type: string
                      tolerationSeconds:
                        description: TolerationSeconds represents the period of time
                          the toleration (which must be of effect NoExecute, otherwise
                          this field is ignored) tolerates the taint. By default,
                          it is not set, which means tolerate the taint forever (do
                          not evict). Zero and negative values will be treated as
                          0 (evict immediately) by the system.
                        format: int64
                        type: integer
                      value:
                        description: Value is the taint value the toleration matches
                          to. If the operator is Exists, the value should be empty,
                          otherwise just a regular string.
                        type: string
                    type: object
                  type: array
              type: object
            remediationEnforcement:
              description: 'Specifies what to do with remediations of Enforcement
                type. If left empty, this defaults to "off" which doesn''t create
                nor apply any enforcement remediations. If set to "all" this creates
                any enforcement remediations it encounters. Subsequently, this can
                also be set to a specific type. e.g. setting it to "gatekeeper" will
                apply any enforcement remediations relevant to the Gatekeeper OPA
                system. These objects will annotated in the content itself with: complianceascode.io/enforcement-type:
                <type>'
              type: string
            roles:
              description: "The list of roles to apply node-specific checks to. \n
                This will be translated to the standard Kubernetes role label `node-role.kubernetes.io/<role
                name>`. \n It's also possible to specify `@all` as a role, which will
                run a scan on all nodes by not specifying a node selector as we normally
                do. The usage of `@all` in OpenShift is discouraged as the operator
                won't be able to apply remediations unless roles are specified. \n
                Note that tolerations must still be configured for the opeartor to
                appropriately schedule scans."
              items:
                type: string
              type: array
            scanLimits:
              additionalProperties:
                anyOf:
                - type: integer
                - type: string
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              description: ScanLimits allows to set the resource limits that the scan
                pods are allowed to use. By default, compliance operator will use
                sensible defaults (500Mi memory, 100m CPU for the scanner container
                and 200Mi memory with 100m CPU for the api-resource-collector container).
              type: object
            scanTolerations:
              default:
              - operator: Exists
              description: Specifies tolerations needed for the scan to run on the
                nodes. This is useful in case the target set of nodes have custom
                taints that don't allow certain workloads to run. Defaults to allowing
                scheduling on all nodes.
              items:
                description: The pod this Toleration is attached to tolerates any
                  taint that matches the triple <key,value,effect> using the matching
                  operator <operator>.
                properties:
                  effect:
                    description: Effect indicates the taint effect to match. Empty
                      means match all taint effects. When specified, allowed values
                      are NoSchedule, PreferNoSchedule and NoExecute.
                    type: string
                  key:
                    description: Key is the taint key that the toleration applies
                      to. Empty means match all taint keys. If the key is empty, operator
                      must be Exists; this combination means to match all values and
                      all keys.
                    type: string
                  operator:
                    description: Operator represents a key's relationship to the value.
                      Valid operators are Exists and Equal. Defaults to Equal. Exists
                      is equivalent to wildcard for value, so that a pod can tolerate
                      all taints of a particular category.
                    type: string
                  tolerationSeconds:
                    description: TolerationSeconds represents the period of time the
                      toleration (which must be of effect NoExecute, otherwise this
                      field is ignored) tolerates the taint. By default, it is not
                      set, which means tolerate the taint forever (do not evict).
                      Zero and negative values will be treated as 0 (evict immediately)
                      by the system.
                    format: int64
                    type: integer
                  value:
                    description: Value is the taint value the toleration matches to.
                      If the operator is Exists, the value should be empty, otherwise
                      just a regular string.
                    type: string
                type: object
              type: array
            schedule:
              description: Defines a schedule for the scans to run. This is in cronjob
                format. Note the scan will still be triggered immediately, and the
                scheduled scans will start running only after the initial results
                are ready.
              type: string
            showNotApplicable:
              default: false
              description: Determines whether to hide or show results that are not
                applicable.
              type: boolean
            strictNodeScan:
              default: true
              description: Defines whether the scan should proceed if we're not able
                to scan all the nodes or not. `true` means that the operator should
                be strict and error out. `false` means that we don't need to be strict
                and we can proceed.
              type: boolean
            suspend:
              default: false
              description: Defines if a schedule should be suspended and is a boolean
                value, defaulting to False.
              type: boolean
            timeout:
              default: 30m
              description: Timeout is the maximum amount of time the scan can run.
                If the scan hasn't finished by then, it will be aborted.
              type: string
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: ScanSetting
      listKind: ScanSettingList
      plural: scansettings
      shortNames:
      - ss
      singular: scansetting
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"tailoredprofiles.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"TailoredProfile","listKind":"TailoredProfileList","plural":"tailoredprofiles","shortNames":["tp","tprof"],"singular":"tailoredprofile"},"scope":"Namespaced","versions":[{"additionalPrinterColumns":[{"description":"State of the tailored profile","jsonPath":".status.state","name":"State","type":"string"}],"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"TailoredProfile is the Schema for the tailoredprofiles API","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"spec":{"description":"TailoredProfileSpec defines the desired state of TailoredProfile","properties":{"description":{"description":"Description of tailored profile. It can't be empty.","pattern":"^.+$","type":"string"},"disableRules":{"description":"Disables the referenced rules","items":{"description":"RuleReferenceSpec specifies a rule to be selected/deselected, as well as the reason why","properties":{"name":{"description":"Name of the rule that's being referenced","type":"string"},"rationale":{"description":"Rationale of why this rule is being selected/deselected","type":"string"}},"required":["name","rationale"],"type":"object"},"nullable":true,"type":"array"},"enableRules":{"description":"Enables the referenced rules","items":{"description":"RuleReferenceSpec specifies a rule to be selected/deselected, as well as the reason why","properties":{"name":{"description":"Name of the rule that's being referenced","type":"string"},"rationale":{"description":"Rationale of why this rule is being selected/deselected","type":"string"}},"required":["name","rationale"],"type":"object"},"nullable":true,"type":"array"},"extends":{"description":"Points to the name of the profile to extend","type":"string"},"manualRules":{"description":"Disables the automated check on referenced rules for manual check","items":{"description":"RuleReferenceSpec specifies a rule to be selected/deselected, as well as the reason why","properties":{"name":{"description":"Name of the rule that's being referenced","type":"string"},"rationale":{"description":"Rationale of why this rule is being selected/deselected","type":"string"}},"required":["name","rationale"],"type":"object"},"nullable":true,"type":"array"},"setValues":{"description":"Sets the referenced variables to selected values","items":{"description":"ValueReferenceSpec specifies a value to be set for a variable with a reason why","properties":{"name":{"description":"Name of the variable that's being referenced","type":"string"},"rationale":{"description":"Rationale of why this value is being tailored","type":"string"},"value":{"description":"Value of the variable being set","type":"string"}},"required":["name","rationale","value"],"type":"object"},"nullable":true,"type":"array"},"title":{"description":"Title for the tailored profile. It can't be empty.","pattern":"^.+$","type":"string"}},"required":["description","title"],"type":"object"},"status":{"description":"TailoredProfileStatus defines the observed state of TailoredProfile","properties":{"errorMessage":{"type":"string"},"id":{"description":"The XCCDF ID of the tailored profile","type":"string"},"outputRef":{"description":"Points to the generated resource","properties":{"name":{"type":"string"},"namespace":{"type":"string"}},"required":["name","namespace"],"type":"object"},"state":{"description":"The current state of the tailored profile","type":"string"},"warnings":{"type":"string"}},"type":"object"}},"type":"object"}},"served":true,"storage":true,"subresources":{"status":{}}}]}}
    creationTimestamp: "2024-02-05T10:27:56Z"
    generation: 1
    name: tailoredprofiles.compliance.openshift.io
    resourceVersion: "39284"
    uid: ea7808b7-7f60-451c-a6a4-3050985e8562
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: TailoredProfile
      listKind: TailoredProfileList
      plural: tailoredprofiles
      shortNames:
      - tp
      - tprof
      singular: tailoredprofile
    scope: Namespaced
    versions:
    - additionalPrinterColumns:
      - description: State of the tailored profile
        jsonPath: .status.state
        name: State
        type: string
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: TailoredProfile is the Schema for the tailoredprofiles API
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            spec:
              description: TailoredProfileSpec defines the desired state of TailoredProfile
              properties:
                description:
                  description: Description of tailored profile. It can't be empty.
                  pattern: ^.+$
                  type: string
                disableRules:
                  description: Disables the referenced rules
                  items:
                    description: RuleReferenceSpec specifies a rule to be selected/deselected,
                      as well as the reason why
                    properties:
                      name:
                        description: Name of the rule that's being referenced
                        type: string
                      rationale:
                        description: Rationale of why this rule is being selected/deselected
                        type: string
                    required:
                    - name
                    - rationale
                    type: object
                  nullable: true
                  type: array
                enableRules:
                  description: Enables the referenced rules
                  items:
                    description: RuleReferenceSpec specifies a rule to be selected/deselected,
                      as well as the reason why
                    properties:
                      name:
                        description: Name of the rule that's being referenced
                        type: string
                      rationale:
                        description: Rationale of why this rule is being selected/deselected
                        type: string
                    required:
                    - name
                    - rationale
                    type: object
                  nullable: true
                  type: array
                extends:
                  description: Points to the name of the profile to extend
                  type: string
                manualRules:
                  description: Disables the automated check on referenced rules for
                    manual check
                  items:
                    description: RuleReferenceSpec specifies a rule to be selected/deselected,
                      as well as the reason why
                    properties:
                      name:
                        description: Name of the rule that's being referenced
                        type: string
                      rationale:
                        description: Rationale of why this rule is being selected/deselected
                        type: string
                    required:
                    - name
                    - rationale
                    type: object
                  nullable: true
                  type: array
                setValues:
                  description: Sets the referenced variables to selected values
                  items:
                    description: ValueReferenceSpec specifies a value to be set for
                      a variable with a reason why
                    properties:
                      name:
                        description: Name of the variable that's being referenced
                        type: string
                      rationale:
                        description: Rationale of why this value is being tailored
                        type: string
                      value:
                        description: Value of the variable being set
                        type: string
                    required:
                    - name
                    - rationale
                    - value
                    type: object
                  nullable: true
                  type: array
                title:
                  description: Title for the tailored profile. It can't be empty.
                  pattern: ^.+$
                  type: string
              required:
              - description
              - title
              type: object
            status:
              description: TailoredProfileStatus defines the observed state of TailoredProfile
              properties:
                errorMessage:
                  type: string
                id:
                  description: The XCCDF ID of the tailored profile
                  type: string
                outputRef:
                  description: Points to the generated resource
                  properties:
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                state:
                  description: The current state of the tailored profile
                  type: string
                warnings:
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
  status:
    acceptedNames:
      kind: TailoredProfile
      listKind: TailoredProfileList
      plural: tailoredprofiles
      shortNames:
      - tp
      - tprof
      singular: tailoredprofile
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
- apiVersion: apiextensions.k8s.io/v1
  kind: CustomResourceDefinition
  metadata:
    annotations:
      controller-gen.kubebuilder.io/version: v0.13.0
      kubectl.kubernetes.io/last-applied-configuration: |
        {"apiVersion":"apiextensions.k8s.io/v1","kind":"CustomResourceDefinition","metadata":{"annotations":{"controller-gen.kubebuilder.io/version":"v0.13.0"},"name":"variables.compliance.openshift.io"},"spec":{"group":"compliance.openshift.io","names":{"kind":"Variable","listKind":"VariableList","plural":"variables","shortNames":["var"],"singular":"variable"},"scope":"Namespaced","versions":[{"name":"v1alpha1","schema":{"openAPIV3Schema":{"description":"Variable describes a tunable in the XCCDF profile","properties":{"apiVersion":{"description":"APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources","type":"string"},"description":{"description":"The description of the Variable","type":"string"},"id":{"description":"the ID of the variable","type":"string"},"kind":{"description":"Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds","type":"string"},"metadata":{"type":"object"},"selections":{"description":"Enumerates what values are allowed for this variable. Can be empty.","items":{"properties":{"description":{"description":"The string description of the selection","type":"string"},"value":{"description":"The value of the variable","type":"string"}},"type":"object"},"nullable":true,"type":"array","x-kubernetes-list-type":"atomic"},"title":{"description":"The title of the Variable","type":"string"},"type":{"description":"The type of the variable","enum":["number","bool","string"],"type":"string"},"value":{"description":"The value of the variable","type":"string"}},"required":["id","title","type"],"type":"object"}},"served":true,"storage":true}]}}
    creationTimestamp: "2024-02-05T10:27:56Z"
    generation: 1
    name: variables.compliance.openshift.io
    resourceVersion: "39289"
    uid: 5ab7af06-221f-41db-b245-41735f9a9cbd
  spec:
    conversion:
      strategy: None
    group: compliance.openshift.io
    names:
      kind: Variable
      listKind: VariableList
      plural: variables
      shortNames:
      - var
      singular: variable
    scope: Namespaced
    versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: Variable describes a tunable in the XCCDF profile
          properties:
            apiVersion:
              description: 'APIVersion defines the versioned schema of this representation
                of an object. Servers should convert recognized schemas to the latest
                internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
              type: string
            description:
              description: The description of the Variable
              type: string
            id:
              description: the ID of the variable
              type: string
            kind:
              description: 'Kind is a string value representing the REST resource
                this object represents. Servers may infer this from the endpoint the
                client submits requests to. Cannot be updated. In CamelCase. More
                info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
              type: string
            metadata:
              type: object
            selections:
              description: Enumerates what values are allowed for this variable. Can
                be empty.
              items:
                properties:
                  description:
                    description: The string description of the selection
                    type: string
                  value:
                    description: The value of the variable
                    type: string
                type: object
              nullable: true
              type: array
              x-kubernetes-list-type: atomic
            title:
              description: The title of the Variable
              type: string
            type:
              description: The type of the variable
              enum:
              - number
              - bool
              - string
              type: string
            value:
              description: The value of the variable
              type: string
          required:
          - id
          - title
          - type
          type: object
      served: true
      storage: true
  status:
    acceptedNames:
      kind: Variable
      listKind: VariableList
      plural: variables
      shortNames:
      - var
      singular: variable
    conditions:
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: no conflicts found
      reason: NoConflicts
      status: "True"
      type: NamesAccepted
    - lastTransitionTime: "2024-02-05T10:27:56Z"
      message: the initial names have been accepted
      reason: InitialNamesAccepted
      status: "True"
      type: Established
    storedVersions:
    - v1alpha1
kind: List
metadata:
  resourceVersion: ""
