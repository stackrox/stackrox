{
  "name": "New User Added",
  "description": "Detects when the 'useradd' or 'adduser' process is used to add a new user to the system",
  "severity": "HIGH_SEVERITY",
  "lifecycle_stages": ["RUN_TIME"],
  "categories": ["System Modification", "Privileges"],
  "fields": {
      "process_policy": {
          "name": "useradd|adduser"
      }
  },
  "rationale": "Users added in run time can be used to take ownership of files and processes",
  "remediation": "Consider using a base image that doesn't have a shell such as SCRATCH or gcr.io/distroless. If not, modify your Dockerfile to use the exec form of CMD/ENTRYPOINT ([\"using braces\"]) instead of the shell form (no braces)"
}
