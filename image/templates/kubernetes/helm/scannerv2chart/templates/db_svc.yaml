apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.dbAppName }}
  namespace: stackrox
  labels:
    app: {{ .Values.dbAppName }}
    app.kubernetes.io/name: stackrox
spec:
  type: {{ .Values.postgresql.service.type }}
  ports:
  - name: http-postgresql
    port: {{ .Values.postgresql.service.port }}
    targetPort: postgresql
  selector:
    app: {{ .Values.dbAppName }}
