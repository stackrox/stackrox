apiVersion: v1
kind: Service
metadata:
  name: scanner
  namespace: {{.Values.namespace}}
spec:
  ports:
  - name: clair-http
    port: 6060
    targetPort: 6060
  - name: clairify-http
    port: 8080
    targetPort: 8080
  selector:
    app: scanner
  type: ClusterIP
