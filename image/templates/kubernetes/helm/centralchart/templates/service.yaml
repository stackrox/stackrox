apiVersion: v1
kind: Service
metadata:
  name: central
  namespace: {{.Values.namespace}}
  labels:
      app.kubernetes.io/name: central
spec:
  ports:
  - name: https
    port: 443
    targetPort: 443
  selector:
    app: central
  type: ClusterIP
