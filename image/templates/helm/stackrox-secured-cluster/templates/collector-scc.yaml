{{- include "srox.init" . -}}

{{- if ._rox.env.openshift }}

{{- if false }}
# "fake" document separator to aid GVK extraction heuristic
---
{{- end }}

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: use-collector-scc
  namespace: {{ ._rox._namespace }}
  labels:
    {{- include "srox.labels" (list . "role" "use-collector-scc") | nindent 4 }}
  annotations:
    {{- include "srox.annotations" (list . "role" "use-collector-scc") | nindent 4 }}
rules:
- apiGroups:
  - security.openshift.io
  resources:
  - securitycontextconstraints
  resourceNames:
  - privileged
  verbs:
  - use
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: collector-use-scc
  namespace: {{ ._rox._namespace }}
  labels:
    {{- include "srox.labels" (list . "rolebinding" "collector-use-scc") | nindent 4 }}
  annotations:
    {{- include "srox.annotations" (list . "rolebinding" "collector-use-scc") | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: use-collector-scc
subjects:
- kind: ServiceAccount
  name: collector
  namespace: {{ ._rox._namespace }}

{{- end }}
