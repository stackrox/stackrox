{{- include "srox.init" . -}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: central-endpoints
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "srox.defaultLabels" . | nindent 4 }}
  annotations:
    {{- include "srox.defaultAnnotations" . | nindent 4 }}
data:
  endpoints.yaml: |
    {{- include "srox.expand" (list . ._rox.central.endpointConfig) | nindent 4 }}
