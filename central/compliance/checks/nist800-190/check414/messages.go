package check414

const interpretationText = `StackRox has policies in place to monitor for secrets being used in an insecure way. 
Therefore, the cluster is compliant if there are policies being enforced that detect such configurations and take
enforcement action to prevent such deployments from running in production.`
