package check24

const interpretationText = `StackRox tracks the Kubernetes objects that exist in your environment, giving you a constant
inventory of what is happening in your clusters.`

func passText() string {
	return "StackRox gives you visibility into all system components in kubernetes."
}
