package check22

const interpretationText = `StackRox provides the ability to run CIS benchmarks on demand, and on a schedule. Therefore,
a cluster is compliant if CIS Docker or CIS Kubernetes benchmarks were run on the cluster.`
