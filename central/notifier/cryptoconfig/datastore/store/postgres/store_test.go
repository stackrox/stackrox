// Code generated by pg-bindings generator. DO NOT EDIT.

//go:build sql_integration

package postgres

import (
	"context"
	"testing"

	"github.com/stackrox/rox/generated/storage"
	"github.com/stackrox/rox/pkg/postgres/pgtest"
	"github.com/stackrox/rox/pkg/sac"
	"github.com/stackrox/rox/pkg/testutils"
	"github.com/stretchr/testify/suite"
)

type NotifierCryptoConfigsStoreSuite struct {
	suite.Suite
	store  Store
	testDB *pgtest.TestPostgres
}

func TestNotifierCryptoConfigsStore(t *testing.T) {
	suite.Run(t, new(NotifierCryptoConfigsStoreSuite))
}

func (s *NotifierCryptoConfigsStoreSuite) SetupTest() {
	s.testDB = pgtest.ForT(s.T())
	s.store = New(s.testDB.DB)
}

func (s *NotifierCryptoConfigsStoreSuite) TearDownTest() {
	s.testDB.Teardown(s.T())
}

func (s *NotifierCryptoConfigsStoreSuite) TestStore() {
	ctx := sac.WithAllAccess(context.Background())

	store := s.store

	notifierCryptoConfig := &storage.NotifierCryptoConfig{}
	s.NoError(testutils.FullInit(notifierCryptoConfig, testutils.SimpleInitializer(), testutils.JSONFieldsFilter))

	foundNotifierCryptoConfig, exists, err := store.Get(ctx)
	s.NoError(err)
	s.False(exists)
	s.Nil(foundNotifierCryptoConfig)

	withNoAccessCtx := sac.WithNoAccess(ctx)

	s.NoError(store.Upsert(ctx, notifierCryptoConfig))
	foundNotifierCryptoConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(notifierCryptoConfig, foundNotifierCryptoConfig)

	foundNotifierCryptoConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(notifierCryptoConfig, foundNotifierCryptoConfig)

	s.NoError(store.Delete(ctx))
	foundNotifierCryptoConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.False(exists)
	s.Nil(foundNotifierCryptoConfig)

	s.ErrorIs(store.Delete(withNoAccessCtx), sac.ErrResourceAccessDenied)

	notifierCryptoConfig = &storage.NotifierCryptoConfig{}
	s.NoError(testutils.FullInit(notifierCryptoConfig, testutils.SimpleInitializer(), testutils.JSONFieldsFilter))
	s.NoError(store.Upsert(ctx, notifierCryptoConfig))

	foundNotifierCryptoConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(notifierCryptoConfig, foundNotifierCryptoConfig)

	notifierCryptoConfig = &storage.NotifierCryptoConfig{}
	s.NoError(testutils.FullInit(notifierCryptoConfig, testutils.SimpleInitializer(), testutils.JSONFieldsFilter))
	s.NoError(store.Upsert(ctx, notifierCryptoConfig))

	foundNotifierCryptoConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(notifierCryptoConfig, foundNotifierCryptoConfig)
}
