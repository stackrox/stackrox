// Code generated by pg-bindings generator. DO NOT EDIT.

//go:build sql_integration

package postgres

import (
	"context"
	"testing"

	"github.com/stackrox/rox/generated/storage"
	"github.com/stackrox/rox/pkg/postgres/pgtest"
	"github.com/stackrox/rox/pkg/sac"
	"github.com/stackrox/rox/pkg/testutils"
	"github.com/stretchr/testify/suite"
)

type DelegatedRegistryConfigsStoreSuite struct {
	suite.Suite
	store  Store
	testDB *pgtest.TestPostgres
}

func TestDelegatedRegistryConfigsStore(t *testing.T) {
	suite.Run(t, new(DelegatedRegistryConfigsStoreSuite))
}

func (s *DelegatedRegistryConfigsStoreSuite) SetupTest() {
	s.testDB = pgtest.ForT(s.T())
	s.store = New(s.testDB.DB)
}

func (s *DelegatedRegistryConfigsStoreSuite) TearDownTest() {
	s.testDB.Teardown(s.T())
}

func (s *DelegatedRegistryConfigsStoreSuite) TestStore() {
	ctx := sac.WithAllAccess(context.Background())

	store := s.store

	delegatedRegistryConfig := &storage.DelegatedRegistryConfig{}
	s.NoError(testutils.FullInit(delegatedRegistryConfig, testutils.SimpleInitializer(), testutils.JSONFieldsFilter))

	foundDelegatedRegistryConfig, exists, err := store.Get(ctx)
	s.NoError(err)
	s.False(exists)
	s.Nil(foundDelegatedRegistryConfig)

	withNoAccessCtx := sac.WithNoAccess(ctx)

	s.NoError(store.Upsert(ctx, delegatedRegistryConfig))
	foundDelegatedRegistryConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(delegatedRegistryConfig, foundDelegatedRegistryConfig)

	foundDelegatedRegistryConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(delegatedRegistryConfig, foundDelegatedRegistryConfig)

	s.NoError(store.Delete(ctx))
	foundDelegatedRegistryConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.False(exists)
	s.Nil(foundDelegatedRegistryConfig)

	s.ErrorIs(store.Delete(withNoAccessCtx), sac.ErrResourceAccessDenied)

	delegatedRegistryConfig = &storage.DelegatedRegistryConfig{}
	s.NoError(testutils.FullInit(delegatedRegistryConfig, testutils.SimpleInitializer(), testutils.JSONFieldsFilter))
	s.NoError(store.Upsert(ctx, delegatedRegistryConfig))

	foundDelegatedRegistryConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(delegatedRegistryConfig, foundDelegatedRegistryConfig)

	delegatedRegistryConfig = &storage.DelegatedRegistryConfig{}
	s.NoError(testutils.FullInit(delegatedRegistryConfig, testutils.SimpleInitializer(), testutils.JSONFieldsFilter))
	s.NoError(store.Upsert(ctx, delegatedRegistryConfig))

	foundDelegatedRegistryConfig, exists, err = store.Get(ctx)
	s.NoError(err)
	s.True(exists)
	s.Equal(delegatedRegistryConfig, foundDelegatedRegistryConfig)
}
