apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  generation: 1
  labels:
    app: ping-server
    name: ping-server
  name: ping-server
  namespace: qa
spec:
  minReadySeconds: 15
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: ping-server
      name: ping-server
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ping-server
        deployment: ping-server
        name: ping-server
      name: ping-server
      namespace: qa
    spec:
      imagePullSecrets:
        - name: qa
      containers:
      - image: quay.io/jvirtane/ping-server:4.8.x-619-g1fb6d87786-dirty-4
        imagePullPolicy: IfNotPresent
        name: external-destination-source
        resources: {}
        securityContext:
          capabilities: {}
          privileged: true
          readOnlyRootFilesystem: false
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
