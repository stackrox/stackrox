apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.JobName}}-berserker-process-config
data:
  workload.toml: |
    restart_interval = 10

    [workload]
    type = "processes"
    arrival_rate = 1000.0
    departure_rate = 200.0
    random_process = true
