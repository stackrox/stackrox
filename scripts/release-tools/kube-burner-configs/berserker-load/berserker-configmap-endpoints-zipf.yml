---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{.JobName}}-berserker-config
data:
  workload.toml: |
    restart_interval = 10
    workers = 9

    [workload]
    type = "endpoints"
    distribution = "zipf"
    n_ports = 2000
    exponent = 1.4
