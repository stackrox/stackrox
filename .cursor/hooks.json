{
  "afterFileEdit": {
    "command": ".claude/hooks/lint.sh ${filePath}",
    "description": "Run the same lint checks as CI (golangci-lint, buf format, shellcheck, prettier) scoped to the edited file"
  }
}
