# History of major version bumps.
# Each entry records the previous major.minor ("from") and the first
# release of the new major ("to"). The "to" minor is always 0.
# Used to compute previous Y-stream versions across major boundaries.
#
# Format note: "from" must appear before "to" within each entry.
# The shell script scripts/get-previous-y-stream.sh parses this file
# with awk and depends on this field ordering.
bumps:
  - from: "0.0"  # 0.0.0 was never released; used for downstream trunk builds
    to: "1.0"
  - from: "3.74"
    to: "4.0"
  - from: "4.11"
    to: "5.0"
