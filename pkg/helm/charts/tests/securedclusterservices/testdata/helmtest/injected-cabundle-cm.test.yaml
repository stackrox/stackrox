tests:
- name: "Injected CA bundle has no data"
  expect: |
    .configmaps["injected-cabundle-stackrox-secured-cluster-services"].stringData | assertThat(. == null)
- name: "Injected CA bundle has a proper label"
  expect: |
    .configmaps["injected-cabundle-stackrox-secured-cluster-services"].metadata.labels["config.openshift.io/inject-trusted-cabundle"] | assertThat(. == "true")
