tests:
- name: collector image
  expect: |
    assertMainIs("test.registry/main:custom-main")
    assertCollectorIs("test.registry/collector:custom-collector")

- name: scanner image
  server:
    visibleSchemas:
    - openshift-4.12
    availableSchemas:
    - openshift-4.12
  set:
    scannerV4.disable: false
  expect: |
    .deployments["scanner-v4-db"] | toyaml | print
    assertScannerIs("test.registry/scanner-v4:custom-scanner")
    assertScannerDBIs("test.registry/scanner-v4-db:custom-scanner")
