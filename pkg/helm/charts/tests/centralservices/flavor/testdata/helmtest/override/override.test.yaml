tests:
- name: should use custom tag
  expect: |
    assertCentral("test.registry/main:custom-main")
    assertScanner("test.registry/scanner-v4:custom-scanner")
    assertScannerDB("test.registry/scanner-v4-db:custom-scanner")
