tests:
- name: should use custom tag
  expect: |
    assertCentral("test.registry/main:custom")
    assertScanner("test.registry/scanner:custom")
    assertScannerDB("test.registry/scanner-db:custom")
