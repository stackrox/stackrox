values:
  central:
    persistence:
      none: true
tests:
- name: should use custom tag
  expect: |
    assertCentral("test.registry/main:custom-main")
    assertScanner("test.registry/scanner:custom-scanner")
    assertScannerDB("test.registry/scanner-db:custom-scanner")
