package schema

// Code not generated by pg-bindings generator. DO EDIT.

import (
	"reflect"
	"time"

	"github.com/stackrox/rox/generated/storage"
	"github.com/stackrox/rox/pkg/postgres"
	"github.com/stackrox/rox/pkg/postgres/walker"
)

var (
	// CreateTableMaximusStmt holds the create statement for table `maximus`.
	CreateTableMaximusStmt = &postgres.CreateStmts{
		GormModel: (*Maximus)(nil),
		Children:  []*postgres.CreateStmts{},
	}

	// MaximusSchema is the go schema for table `maximus`.
	MaximusSchema = func() *walker.Schema {
		schema := GetSchemaForTable("maximus")
		if schema != nil {
			return schema
		}
		schema = walker.Walk(reflect.TypeOf((*storage.Maximus)(nil)), "maximus")
		RegisterTable(schema, CreateTableMaximusStmt)
		return schema
	}()
)

const (
	// MaximusTableName specifies the name of the table in postgres.
	MaximusTableName = "maximus"
)

// Maximus holds the Gorm model for Postgres table `maximus`.
type Maximus struct {
	Metric string    `gorm:"column:metric;type:varchar;primaryKey"`
	Value  int       `gorm:"column:value;type:int"`
	Ts     time.Time `gorm:"column:ts;type:timestamp"`
}
